# Project memory — Territory Developer

Ephemeral cross-session project state only: active blockers, sprint decisions, in-flight initiatives. Behavioral directives → `ia/rules/agent-principles.md` (force-loaded). Domain tips → domain rule files (`ia/rules/`, `web/README.md`). One line per entry.

Format: `- [Title] — one-line hook`

## Active project state

<!-- Add entries here for truly ephemeral cross-session context: blocked stages, live decisions, pending handoffs. Delete when resolved. -->
