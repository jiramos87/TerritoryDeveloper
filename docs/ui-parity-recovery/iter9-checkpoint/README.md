# iter-9 acceptable visual checkpoint — screenshot drop folder

Drop the following 4 PNGs (taken from Editor Play Mode) into this folder so the
`docs/ui-toolkit-parity-recovery-plan.html` §15.2 image references resolve:

1. `cityscene-hud-toolbar.png` — CityScene with HUD strip + bottom-left toolbar
   + picker visible (no modal open).
2. `pause-menu-open.png` — pause menu opened via Esc, centered cream card with
   Resume / Save game / Settings / Exit to main menu buttons.
3. `stats-panel-open.png` — stats panel opened via STATS button, centered cream
   card with header / close-X / 3 tab strip / chart placeholder.
4. `picker-residential.png` — toolbar Residential tile clicked, picker open
   bottom-left showing 3 zoning tier cards (Light / Medium / Heavy).

These are the "after" reference for the iter-2 → iter-9 visual recovery
checkpoint. The "before" reference is at
`tools/reports/bridge-screenshots/rebake-new-game-20260512-213810.png` etc.
