{
  "name": "@territory/mcp-ia-server",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "description": "MCP server exposing Territory Developer IA docs (specs, rules, root markdown).",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "verify": "tsx scripts/verify-mcp.ts",
    "test": "node --import tsx --test tests/parser/*.test.ts tests/tools/*.test.ts",
    "test:watch": "node --import tsx --test --watch tests/parser/*.test.ts tests/tools/*.test.ts",
    "test:coverage": "c8 --src src --include 'src/parser/**/*.ts' --exclude '**/*.test.ts' --check-coverage --lines 90 --reporter text node --import tsx --test tests/parser/*.test.ts tests/tools/*.test.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "gray-matter": "^4.0.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "c8": "^10.1.3",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  }
}
