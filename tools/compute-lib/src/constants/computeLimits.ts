/**
 * Default limits for territory-ia computational tools (TECH-39 §5.2).
 */

/** Default maximum map width/height (cells) for grid-indexed tools. */
export const DEFAULT_MAX_GRID_DIMENSION = 256;

/** Maximum poles / centroids for multipolar growth ring classification. */
export const MAX_CENTROIDS = 16;
