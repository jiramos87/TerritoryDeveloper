id: TECH-79
type: tooling / agent enablement
title: "**Agent memory across sessions** (persistent agent context)"
priority: medium
status: open
section: IA evolution lane
spec: ia/projects/TECH-79.md
files: 
  - tools/mcp-ia-server/src/
  - db/migrations/
  - docs/mcp-ia-server.md
notes: |
  Log MCP tool calls per issue in `agent_session_log` Postgres table. New `ia_recommend(issue_id?, domain?)` MCP tool uses historical patterns to recommend spec sections, glossary terms, and tool sequences. Fire-and-forget logging — never blocks tool responses.
acceptance: |
  tool calls transparently logged; `ia_recommend` returns recommendations based on historical data; graceful `db_unconfigured` degradation
depends_on: []
depends_on_raw: none
related: []
created: 2026-04-17
raw_markdown: |
  - [ ] **TECH-79** — **Agent memory across sessions** (persistent agent context)
    - Type: tooling / agent enablement
    - Files: `tools/mcp-ia-server/src/` (logging middleware + new tool); `db/migrations/`; `docs/mcp-ia-server.md`
    - Spec: `ia/projects/TECH-79.md`
    - Notes: Log MCP tool calls per issue in `agent_session_log` Postgres table. New `ia_recommend(issue_id?, domain?)` MCP tool uses historical patterns to recommend spec sections, glossary terms, and tool sequences. Fire-and-forget logging — never blocks tool responses.
    - Acceptance: tool calls transparently logged; `ia_recommend` returns recommendations based on historical data; graceful `db_unconfigured` degradation
    - Depends on: none
