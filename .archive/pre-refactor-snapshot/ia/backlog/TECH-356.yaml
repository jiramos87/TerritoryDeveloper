id: TECH-356
type: infrastructure / scripting (test)
title: Concurrency test `materialize-concurrent.sh` (Stage 2.1 Phase 2)
priority: medium
status: open
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-356.md
files:
  - tools/scripts/test/materialize-concurrent.sh
notes: |
  Author `tools/scripts/test/materialize-concurrent.sh` — spawn N=8 parallel `materialize-backlog.sh` invocations against a temp repo root (same `IA_REPO_ROOT` / tmpdir override pattern as `reserve-id-concurrent.sh`); after all complete, diff resulting BACKLOG.md + BACKLOG-ARCHIVE.md against a serial baseline regen; fail on any diff. Mirrors `tools/scripts/test/reserve-id-concurrent.sh` structure. Proves flock guard from TECH-355 serializes parallel writers correctly (byte-identical output vs serial baseline).
acceptance: |
  `tools/scripts/test/materialize-concurrent.sh` exists + executable; runs N=8 parallel `materialize-backlog.sh`; diffs BACKLOG.md + BACKLOG-ARCHIVE.md byte-identical to serial baseline; exits non-zero on diff; mirrors `reserve-id-concurrent.sh` shape.
depends_on:
  - TECH-355
depends_on_raw: TECH-355 (flock guard must exist to test — hard gate)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-356** — Concurrency test `materialize-concurrent.sh` (Stage 2.1 Phase 2)
    - Type: infrastructure / scripting (test)
    - Files: `tools/scripts/test/materialize-concurrent.sh`
    - Spec: `ia/projects/TECH-356.md`
    - Notes: N=8 parallel `materialize-backlog.sh` against temp repo root; diff BACKLOG.md + BACKLOG-ARCHIVE.md vs serial baseline; fail on any diff. Mirrors `reserve-id-concurrent.sh`.
    - Acceptance: script exists + exec; parallel N=8; byte-identical vs serial baseline; non-zero on diff.
    - Depends on: TECH-355 (flock guard — hard gate)
    - Related: TECH-355, TECH-357
parent_plan: ia/projects/backlog-yaml-mcp-alignment-master-plan.md
task_key: T2.1.3
