id: TECH-80
type: tooling / agent enablement
title: "**Bidirectional IA**: agents propose **glossary** additions and flag **spec** ambiguity"
priority: medium
status: open
section: IA evolution lane
spec: ia/projects/TECH-80.md
files: 
  - tools/mcp-ia-server/src/
  - db/migrations/
  - docs/mcp-ia-server.md
notes: |
  `suggest_ia_improvement(kind, content, context?)` for agents to propose glossary additions, flag spec ambiguity, or suggest invariant additions. `ia_suggestions_pending` and `ia_suggestion_resolve` for human review lifecycle (`proposed` → `accepted` / `rejected`). Human review mandatory.
acceptance: |
  three tools registered; full lifecycle (propose → list → resolve) works; `npm run verify` green
depends_on: []
depends_on_raw: none
related: []
created: 2026-04-17
raw_markdown: |
  - [ ] **TECH-80** — **Bidirectional IA**: agents propose **glossary** additions and flag **spec** ambiguity
    - Type: tooling / agent enablement
    - Files: `tools/mcp-ia-server/src/` (3 new tools); `db/migrations/`; `docs/mcp-ia-server.md`
    - Spec: `ia/projects/TECH-80.md`
    - Notes: `suggest_ia_improvement(kind, content, context?)` for agents to propose glossary additions, flag spec ambiguity, or suggest invariant additions. `ia_suggestions_pending` and `ia_suggestion_resolve` for human review lifecycle (`proposed` → `accepted` / `rejected`). Human review mandatory.
    - Acceptance: three tools registered; full lifecycle (propose → list → resolve) works; `npm run verify` green
    - Depends on: none
