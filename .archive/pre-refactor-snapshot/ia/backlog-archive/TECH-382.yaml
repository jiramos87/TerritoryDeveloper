id: TECH-382
type: tech (audit — cross-file spend call sites)
title: Audit cross-file `SpendMoney` + `currentMoney` call sites (Stage 1.2 Phase 2)
priority: medium
status: closed
section: Recent archive
spec: (removed after closure)
files:
  - Assets/Scripts/
notes: |
  Completed (verified — `/project-spec-close`): documentation-only task — re-ran greps `currentMoney\s*-=` / `currentMoney\s*=` / `SpendMoney(` / `RemoveMoney` across `Assets/Scripts/**`. §6 Decision Log audit table filled with 15 rows classified by disposition. Kickoff grep was incomplete (only `currentMoney` + `SpendMoney(`); re-run added `RemoveMoney` pattern and discovered 5 active cross-file drift sites bypassing `TreasuryFloorClampService.TrySpend` — `BuildingPlacementService.cs:246`, `ZoneManager.cs:725`, `GridManager.cs:870`, `GrowthBudgetManager.cs:73`, `RoadManager.cs:242`. `UrbanizationProposalManager.cs:266` accepted as-is (invariant #11 obsolete). Drift sites require `EconomyManager` ref injection — flagged as follow-on work outside Stage 1.2 scope. Decision Log persisted to `ia_project_spec_journal`.
acceptance: |
  grep `currentMoney \-=` across `Assets/Scripts/` returns only `TreasuryFloorClampService.cs` hits; grep `currentMoney =` (assignment, not comparison) returns only initialization + `TreasuryFloorClampService` rewrites; audit table in `TECH-382.md` §6 Decision Log lists every pre-audit hit + disposition; `unity:compile-check` green
depends_on:
  - TECH-381
depends_on_raw: "**TECH-381** (archived)"
related: []
created: 2026-04-18
closed: 2026-04-18
raw_markdown: |
  - [x] **TECH-382** — Audit cross-file `SpendMoney` + `currentMoney` call sites (Stage 1.2 Phase 2) (2026-04-18)
    - Type: tech (audit — cross-file spend call sites)
    - Files: `Assets/Scripts/**`
    - Spec: (removed after closure)
    - Notes: Completed (verified — `/project-spec-close`): documentation-only task — re-ran greps `currentMoney\s*-=` / `currentMoney\s*=` / `SpendMoney(` / `RemoveMoney` across `Assets/Scripts/**`. §6 Decision Log audit table filled with 15 rows classified by disposition. Kickoff grep was incomplete (only `currentMoney` + `SpendMoney(`); re-run added `RemoveMoney` pattern and discovered 5 active cross-file drift sites bypassing `TreasuryFloorClampService.TrySpend` — `BuildingPlacementService.cs:246`, `ZoneManager.cs:725`, `GridManager.cs:870`, `GrowthBudgetManager.cs:73`, `RoadManager.cs:242`. `UrbanizationProposalManager.cs:266` accepted as-is (invariant #11 obsolete). Drift sites require `EconomyManager` ref injection — flagged as follow-on work outside Stage 1.2 scope. Decision Log persisted to `ia_project_spec_journal`.
    - Acceptance: grep `currentMoney \-=` across `Assets/Scripts/` returns only `TreasuryFloorClampService.cs` hits; grep `currentMoney =` (assignment, not comparison) returns only initialization + `TreasuryFloorClampService` rewrites; audit table in `TECH-382.md` §6 Decision Log lists every pre-audit hit + disposition; `unity:compile-check` green
    - Depends on: **TECH-381** (archived)
parent_plan: ia/projects/zone-s-economy-master-plan.md
task_key: T1.2.4
