id: TECH-377
type: web / code + tests
title: "**Web** — `design-tokens.ts` + unit tests (Stage 8.1 T8.1.3)"
priority: medium
status: closed
section: Web platform
spec: ""
files: 
  - web/lib/design-tokens.ts
  - web/lib/__tests__/design-tokens.test.ts
notes: |
  Author `web/lib/design-tokens.ts` — nested TS `const as const`: `typeScale` (10 entries), `spacing` (9 entries), `motion` (4 durations + `reducedMotion: { duration: 0 }`), `text` + `surface` + `accent` semantic alias maps; imports `./tokens/palette.json`; zero palette mutation. JSDoc on `motion.reducedMotion`: `prefers-reduced-motion: reduce` collapses all durations to 0 via CSS media query in `globals.css`. Author `web/lib/__tests__/design-tokens.test.ts` — assert typeScale monotonically decreasing rem, 9 spacing stops, motion keys complete, `reducedMotion.duration === 0`, alias hex values resolve to palette raw entries. Stage 8.1 T8.1.3 of `ia/projects/web-platform-master-plan.md`.
acceptance: |
  `design-tokens.ts` exports `typeScale` + `spacing` + `motion` + `text` + `surface` + `accent` as `const`; tests assert monotonicity + cardinality + alias resolution + `reducedMotion.duration === 0`; `npm run validate:web` + `npm run validate:all` green.
depends_on: 
  - TECH-375
  - TECH-376
depends_on_raw: TECH-375 + TECH-376 (design-system.md §1–§4 source-of-truth for scale + alias selection — hard)
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-377** — **Web** — `design-tokens.ts` + unit tests (Stage 8.1 T8.1.3) (2026-04-18)
    - Type: web / code + tests
    - Files: `web/lib/design-tokens.ts`, `web/lib/__tests__/design-tokens.test.ts`
    - Spec: (removed — unfiled via TECH-411 Phase 1 reset)
    - Notes: `const as const` TS exports for `typeScale` (10) + `spacing` (9) + `motion` (4 + reducedMotion) + `text/surface/accent` aliases; imports `palette.json`; zero mutation. Tests: monotonic typeScale rem, 9 spacing stops, motion keys complete, `reducedMotion.duration === 0`, alias hex resolves to palette.
    - Acceptance: exports + tests green; `validate:web` + `validate:all` green.
    - Depends on: TECH-375, TECH-376 (spec source-of-truth — hard)
    - Related: TECH-375, TECH-376, TECH-378
