id: TECH-364
type: infrastructure / MCP tooling
title: Map v2 locator fields in yaml read path (Stage 3.1 Phase 1)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-364.md
files: 
  - tools/mcp-ia-server/src/parser/backlog-yaml-loader.ts
notes: |
  In `yamlToIssue`, populate 9 new fields from yaml. Arrays default `[]`; scalars default `null`. Add regex guard on `task_key`: reject on parse when present + not matching `^T\d+\.\d+(\.\d+)?$`. v1 records (no locator fields) round-trip unchanged. Source: Step 3 Stage 3.1 Phase 1.
acceptance: |
  Loader populates `parent_plan` / `task_key` / `step` / `stage` / `phase` / `router_domain` / `surfaces` / `mcp_slices` / `skill_hints` from yaml; regex rejects bad `task_key`; v1 fixture still loads; `validate:all` green.
depends_on: 
  - TECH-363
depends_on_raw: TECH-363 (type surface — hard gate)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-364** — Map v2 locator fields in yaml read path (Stage 3.1 Phase 1)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/src/parser/backlog-yaml-loader.ts`
    - Spec: `ia/projects/TECH-364.md`
    - Notes: `yamlToIssue` sets 9 fields; regex guard on `task_key` (`^T\d+\.\d+(\.\d+)?$`); v1 records still load clean.
    - Acceptance: read-path populates fields; bad regex rejected; v1 compat preserved; `validate:all` green.
    - Depends on: TECH-363 (type — hard gate)
    - Related: TECH-363, TECH-365, TECH-366
