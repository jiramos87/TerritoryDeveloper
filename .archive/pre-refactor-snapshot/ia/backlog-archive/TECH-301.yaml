id: TECH-301
type: test / regression
title: Round-trip soft-dep marker integration test (Stage 1.1 Phase 3)
priority: high
status: closed
completed: 2026-04-17
section: Recent archive
spec: (removed at closeout — test-regression issue; no Lessons Learned; Decision Log in git history)
files:
  - tools/mcp-ia-server/tests/tools/backlog-issue.test.ts
notes: |
  Integration test at MCP-tool layer — loads tmp-root yaml fixture w/ `depends_on_raw: "soft: FEAT-12"`, calls `parseBacklogIssue` + `resolveDependsOnStatus`, asserts `soft_only: true`. Plain-id counter-fixture asserts `soft_only: false`. `[optional]` marker deferred — parser has no classifier. Prevents regression of TECH-297 fallback fix. `npm run validate:all` green.
acceptance: |
  Two integration tests cover soft + plain fixtures via tmp-root pattern; regression guard catches lossy `array.join` revert; `npm run validate:all` green.
depends_on:
  - TECH-297
depends_on_raw: TECH-297
related:
  - TECH-297
  - TECH-300
created: 2026-04-17
raw_markdown: |
  - [x] **TECH-301** — Round-trip soft-dep marker integration test (Stage 1.1 Phase 3) (2026-04-17)
    - Type: test / regression
    - Files: `tools/mcp-ia-server/tests/tools/backlog-issue.test.ts`
    - Spec: (removed at closeout — test-regression issue; no Lessons Learned; Decision Log in git history)
    - Notes: Integration test at MCP-tool layer — tmp-root yaml fixtures (`soft: FEAT-12` + plain), asserts `soft_only` classifies correctly. `[optional]` deferred. Regression guard for TECH-297. `npm run validate:all` green.
    - Acceptance: Two tests cover soft + plain; guards lossy `array.join` revert; validate green.
    - Depends on: TECH-297
