id: TECH-380
type: tech (scene / component wiring)
title: "Wire `TreasuryFloorClampService` on `EconomyManager` GameObject (Stage 1.2 Phase 1)"
priority: high
status: open
section: High Priority
spec: ia/projects/TECH-380.md
files:
  - Assets/Scripts/Managers/GameManagers/EconomyManager.cs
  - Assets/Scenes/MainScene.unity
notes: |
  Add `[SerializeField] private TreasuryFloorClampService treasuryFloorClamp;` field to `EconomyManager` + `FindObjectOfType<TreasuryFloorClampService>()` fallback in `EconomyManager.Awake` (guardrail #1, invariant #4 no new singletons). Attach `TreasuryFloorClampService` component to the `EconomyManager` GameObject in main scene prefab; populate Inspector ref. Document composition relationship in XML doc on the field. Does not re-route spend calls yet — wiring only.
acceptance: |
  EconomyManager holds non-null `treasuryFloorClamp` after `Awake` whether Inspector ref set or cleared; scene loads without null refs; `unity:compile-check` green
depends_on:
  - TECH-379
depends_on_raw: "**TECH-379** (archived)"
related:
  - TECH-381
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-380** — Wire `TreasuryFloorClampService` on `EconomyManager` GameObject (Stage 1.2 Phase 1)
    - Type: tech (scene / component wiring)
    - Files: `Assets/Scripts/Managers/GameManagers/EconomyManager.cs`, `Assets/Scenes/MainScene.unity`
    - Spec: `ia/projects/TECH-380.md`
    - Notes: Add `[SerializeField] private TreasuryFloorClampService treasuryFloorClamp;` field to `EconomyManager` + `FindObjectOfType<TreasuryFloorClampService>()` fallback in `EconomyManager.Awake` (guardrail #1, invariant #4 no new singletons). Attach `TreasuryFloorClampService` component to the `EconomyManager` GameObject in main scene prefab; populate Inspector ref. Document composition relationship in XML doc on the field. Does not re-route spend calls yet — wiring only.
    - Acceptance: EconomyManager holds non-null `treasuryFloorClamp` after `Awake` whether Inspector ref set or cleared; scene loads without null refs; `unity:compile-check` green
    - Depends on: **TECH-379** (archived)
