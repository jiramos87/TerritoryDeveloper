id: TECH-365
type: infrastructure / MCP tooling
title: Emit v2 locator fields in yaml writer path (Stage 3.1 Phase 2)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-365.md
files: 
  - tools/mcp-ia-server/src/parser/backlog-yaml-loader.ts
  - tools/scripts/migrate-backlog-to-yaml.mjs
notes: |
  In `buildYaml` (or equivalent writer) + `tools/scripts/migrate-backlog-to-yaml.mjs`: emit `parent_plan` + `task_key` + 7 optional fields when present. Omit absent — no empty arrays, no `null:` keys. Preserve existing section order + block-literal style. Round-trip byte-identical for v1 records. Source: Step 3 Stage 3.1 Phase 2.
acceptance: |
  Writer emits v2 fields when present, omits when absent; v1 records round-trip byte-identical; existing section order + block literals preserved; `validate:all` green.
depends_on: 
  - TECH-364
depends_on_raw: TECH-364 (read path — hard gate for round-trip verification)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-365** — Emit v2 locator fields in yaml writer path (Stage 3.1 Phase 2)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/src/parser/backlog-yaml-loader.ts`, `tools/scripts/migrate-backlog-to-yaml.mjs`
    - Spec: `ia/projects/TECH-365.md`
    - Notes: Emit 9 fields when present; omit absent; preserve section order + block literal style; v1 round-trip byte-identical.
    - Acceptance: writer emits / omits correctly; v1 round-trip byte-identical; `validate:all` green.
    - Depends on: TECH-364 (read path — hard gate)
    - Related: TECH-363, TECH-364, TECH-366
