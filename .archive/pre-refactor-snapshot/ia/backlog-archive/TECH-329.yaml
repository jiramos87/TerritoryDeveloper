id: TECH-329
type: infrastructure / MCP tooling
title: Test `backlog_list` filter combinations (Stage 1.2 Phase 3)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ""
files: 
  - tools/mcp-ia-server/tests/tools/backlog-list.test.ts
notes: |
  Shipped `backlog_list` MCP tool test coverage. Two-tier strategy: unit tier imports exported `applyFilters` + `compareIssues` from `backlog-list.ts` and drives pure filter matrix + ordering assertions; integration tier uses `mkdtempSync` + `REPO_ROOT` env override to write yaml fixtures under `{tmp}/ia/backlog/` + `{tmp}/ia/backlog-archive/` and exercise `scope` switch end-to-end. Unit cases U1–U8 cover no-filter, section/priority/type/status singletons (case-insensitive), multi-filter AND, empty result, `compareIssues` ordering (prefix asc, numeric desc). Integration I1–I3 cover `open`/`archive`/`all` scope. Locks filter semantics before `backlog_search` filter extensions in Stage 2.3. Closes Phase 3 of Stage 1.2.
acceptance: |
  Test file covers each filter dim + combos + scope switch + empty + ordering; `npm run validate:all` green.
depends_on: 
  - TECH-328
depends_on_raw: TECH-328 (list tool must exist to test — hard gate)
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-329** — Test `backlog_list` filter combinations (Stage 1.2 Phase 3)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/tests/tools/backlog-list.test.ts`
    - Spec: (removed after closure)
    - Notes: Shipped `backlog_list` test coverage — unit tier (pure `applyFilters` + `compareIssues`) + integration tier (tmpdir yaml fixtures via `REPO_ROOT`). U1–U8 filter matrix + ordering; I1–I3 scope switch. Locks filter semantics before Stage 2.3 `backlog_search` extensions. Closes Phase 3 of Stage 1.2.
    - Acceptance: each filter dim + combos + scope + empty + ordering covered; `validate:all` green.
    - Depends on: TECH-328 (list tool — hard gate)
    - Related: TECH-328
