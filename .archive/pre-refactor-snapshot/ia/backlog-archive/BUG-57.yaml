id: BUG-57
type: bug (lifecycle tooling / orchestrator sync)
title: "`/closeout` must sync step/stage header Status prose from task-table ground truth when last task archives"
priority: closed
status: closed
section: Completed (moved from BACKLOG.md, 2026-04-17)
spec: (removed after closure)
files:
  - ia/skills/project-spec-close/SKILL.md
  - ia/skills/project-stage-close/SKILL.md
  - .claude/agents/closeout.md
  - tools/mcp-ia-server/src/parser/master-plan-header-sync.ts
  - tools/mcp-ia-server/tests/parser/master-plan-header-sync.test.ts
notes: |
  Shipped header-sync helper — `/closeout` + `project-stage-close` now rewrite `**Status:**` + `**Backlog state ...**` paragraphs under `### Step N` / `#### Stage N.N` from task-table ground truth. Terminal flip → `Final`; intermediate refresh → `In Progress — {first-open-task-id}` + `k filed` count. Idempotent (zero diff on already-synced blocks). Phase-1 regex + rewrite helper landed in `tools/mcp-ia-server/src/parser/master-plan-header-sync.ts`; wiring landed in `ia/skills/project-spec-close/SKILL.md` step 6, `ia/skills/project-stage-close/SKILL.md` step 1, `.claude/agents/closeout.md`. Fixture cases (a)–(d) under `tools/mcp-ia-server/tests/parser/master-plan-header-sync.test.ts`.
acceptance: |
  Last-task closeout flips stage/step Status paragraph → `Final`; intermediate refresh updates `k filed` + active-task suffix; rewrite idempotent; parser fixture green; `npm run validate:all` green.
depends_on: []
depends_on_raw: "none (soft: TECH-322 shares parser family)"
related:
  - TECH-322
created: 2026-04-17
closed: 2026-04-17
raw_markdown: |
  - [x] **BUG-57** — `/closeout` must sync step/stage header Status prose from task-table ground truth when last task archives
    - Type: bug (lifecycle tooling / orchestrator sync)
    - Files: `ia/skills/project-spec-close/SKILL.md`, `ia/skills/project-stage-close/SKILL.md`, `.claude/agents/closeout.md`, `tools/mcp-ia-server/src/parser/master-plan-header-sync.ts`, `tools/mcp-ia-server/tests/parser/master-plan-header-sync.test.ts`
    - Notes: Header-sync helper rewrites `**Status:**` + `**Backlog state ...**` paragraphs under step/stage headings from task-table ground truth — terminal → `Final`; intermediate → `In Progress — {first-open}` + `k filed`; idempotent. Wired into `project-spec-close` step 6 + `project-stage-close` step 1 + `closeout` agent seed.
    - Acceptance: terminal flip → `Final`; intermediate refresh updates `k filed` + suffix; idempotent; fixture (a)–(d) green; `npm run validate:all` green.
    - Related: TECH-322
