/** Archetypes catalog sentinel — Stage 7.3 owns the surface (TECH-1614). */
export default function ArchetypesCatalogPage() {
  return (
    <div data-testid="catalog-sentinel-archetypes">
      <h1 className="text-[length:var(--ds-font-size-h2)] font-semibold">Archetypes</h1>
      <p className="text-[var(--ds-text-muted)]">Archetypes catalog — Stage 7.3 authors this surface.</p>
    </div>
  );
}
