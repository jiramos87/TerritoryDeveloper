/** Panels catalog sentinel — Stage 6.4 owns the surface (TECH-1614). */
export default function PanelsCatalogPage() {
  return (
    <div data-testid="catalog-sentinel-panels">
      <h1 className="text-[length:var(--ds-font-size-h2)] font-semibold">Panels</h1>
      <p className="text-[var(--ds-text-muted)]">Panels catalog — Stage 6.4 authors this surface.</p>
    </div>
  );
}
