/** Assets catalog sentinel — Stage 6.2 owns the surface (TECH-1614). */
export default function AssetsCatalogPage() {
  return (
    <div data-testid="catalog-sentinel-assets">
      <h1 className="text-[length:var(--ds-font-size-h2)] font-semibold">Assets</h1>
      <p className="text-[var(--ds-text-muted)]">Assets catalog — Stage 6.2 authors this surface.</p>
    </div>
  );
}
