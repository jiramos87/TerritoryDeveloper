/** Buttons catalog sentinel — Stage 6.3 owns the surface (TECH-1614). */
export default function ButtonsCatalogPage() {
  return (
    <div data-testid="catalog-sentinel-buttons">
      <h1 className="text-[length:var(--ds-font-size-h2)] font-semibold">Buttons</h1>
      <p className="text-[var(--ds-text-muted)]">Buttons catalog — Stage 6.3 authors this surface.</p>
    </div>
  );
}
