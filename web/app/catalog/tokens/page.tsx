/** Tokens catalog sentinel — Stage 7.2 owns the surface (TECH-1614). */
export default function TokensCatalogPage() {
  return (
    <div data-testid="catalog-sentinel-tokens">
      <h1 className="text-[length:var(--ds-font-size-h2)] font-semibold">Tokens</h1>
      <p className="text-[var(--ds-text-muted)]">Tokens catalog — Stage 7.2 authors this surface.</p>
    </div>
  );
}
