# DATABASE_URL — required by /dashboard + /api/ia/* routes (DB-backed reads).
DATABASE_URL=postgresql://postgres:postgres@localhost:5434/territory_ia_dev

# Optional: DASHBOARD_AUTH_SKIP=1 bypasses dashboard auth gate locally.
# DASHBOARD_AUTH_SKIP=1
