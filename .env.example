# Template only — copy to `.env` at the repo root (gitignored) for local values.
# Do not put secrets or production URIs in committed files. Full variable list and defaults live in your `.env`.
# Connection and tooling: docs/postgres-ia-dev-setup.md, docs/mcp-ia-server.md
