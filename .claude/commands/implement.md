---
description: Execute a project spec's Implementation Plan (Stage 4 stub — wires to spec-implementer subagent)
argument-hint: "{ISSUE_ID} (e.g. TECH-85)"
---

# /implement — Stage 1 stub

This slash command is a **Stage 1 stub** for [TECH-85](../../ia/projects/TECH-85-ia-migration.md). The real implementation lands in **Stage 4 / Phase 4.3**, when it will dispatch the `spec-implementer` subagent against `ia/projects/{ID}*.md` in isolated context.

**Not yet wired — coming in Stage 4.**

For now, run the implementation workflow inline using:

- [`.cursor/skills/project-spec-implement/SKILL.md`](../../.cursor/skills/project-spec-implement/SKILL.md)
- territory-ia MCP tools per the per-phase recipe

Argument received: `$ARGUMENTS`
