namespace Territory.UI.StudioControls
{
    /// <summary>Marker interface for typed StudioControl detail-row classes baked from IR `interactives[].detail`.</summary>
    public interface IDetailRow
    {
    }
}
