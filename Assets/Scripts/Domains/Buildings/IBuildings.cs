namespace Domains.Buildings
{
    /// <summary>
    /// Public facade interface for the Buildings domain. Consumers bind to this interface only
    /// — never to concrete service classes directly.
    /// Stage 2.0b: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface IBuildings
    {
    }
}
