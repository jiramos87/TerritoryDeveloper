namespace Domains.Notifications
{
    /// <summary>
    /// Public facade interface for the Notifications domain. Consumers bind to this interface only
    /// — never to GameNotificationManager or concrete service classes directly.
    /// Stage 2.0d: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface INotifications
    {
    }
}
