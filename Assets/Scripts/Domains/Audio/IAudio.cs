namespace Domains.Audio
{
    /// <summary>
    /// Public facade interface for the Audio domain. Consumers bind to this interface only
    /// — never to BlipVoice or concrete service classes directly.
    /// Stage 2.0c: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface IAudio
    {
    }
}
