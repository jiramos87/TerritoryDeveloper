namespace Domains.Save
{
    /// <summary>
    /// Public facade interface for the Save domain. Consumers bind to this interface only
    /// — never to GameSaveManager or concrete service classes directly.
    /// Stage 2.0e: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface ISave
    {
    }
}
