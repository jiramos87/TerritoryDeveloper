namespace Domains.Demand
{
    /// <summary>
    /// Public facade interface for the Demand domain. Consumers bind to this interface only
    /// — never to DemandManager or concrete service classes directly.
    /// Stage 2.0g: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface IDemand
    {
    }
}
