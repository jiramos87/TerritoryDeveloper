namespace Domains.Camera
{
    /// <summary>
    /// Public facade interface for the Camera domain. Consumers bind to this interface only
    /// — never to CameraManager or concrete service classes directly.
    /// Stage 2.0f: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface ICamera
    {
    }
}
