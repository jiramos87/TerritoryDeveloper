namespace Domains.Forests
{
    /// <summary>
    /// Public facade interface for the Forests domain. Consumers bind to this interface only
    /// — never to ForestManager or concrete service classes directly.
    /// Stage 2.0a: scaffold; methods grown per consumer at cutover stages.
    /// </summary>
    public interface IForests
    {
    }
}
