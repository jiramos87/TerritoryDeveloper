---
description: "Sonnet pair-tail skill (seam #4). Reads §Stage Closeout Plan tuple list emitted by stage-closeout-plan (Opus pair-head) from master plan Stage block. Applies bulk: (a) shared glossary / rule / doc migration ops once across the Stage; (b) loops N Task per-Task ops — archive_record (yaml → ia/backlog-archive/ with status: closed + completed: {ISO_DATE}), delete_file (spec), replace_section (task-row Status → Done (archived)), id_purge (durable doc refs), digest_emit (stage_closeout_digest MCP call); (c) runs materialize-backlog.sh + validate:all once at end; (d) aggregates N per-Task digests into one Stage-level digest (chain-level); (e) flips Stage header Status → Final + rolls up to Step / Plan-level Final via R5 gate. Never reorders tuples. Escalates to Opus on anchor ambiguity, validator failure, or transient flock timeout (1 retry). Idempotent: re-run on partial or fully-applied state = zero diff. Triggers: \"stage-closeout-apply\", \"/closeout {MASTER_PLAN_PATH} {STAGE_ID}\", \"apply stage closeout plan\", \"pair-tail stage closeout\", \"bulk close stage apply\". Argument order (explicit): MASTER_PLAN_PATH first, STAGE_ID second."
alwaysApply: false
---
Read and follow: @ia/skills/stage-closeout-apply/SKILL.md

When this skill invokes MCP mutation tools, pass `caller_agent: "closeout"`.
If this skill is lifecycle-heavy, check: @.cursor/rules/cursor-lifecycle-adapters.mdc
