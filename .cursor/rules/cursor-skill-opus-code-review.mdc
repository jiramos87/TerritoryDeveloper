---
description: "Reviewer skill. Runs once per Stage in `/ship-stage` Pass B (after verify-loop on cumulative Stage diff). Reads Stage diff vs combined §Plan Digest acceptance for all Tasks of the Stage, invariants subset, glossary anchors. Three verdict branches: (a) PASS → write §Code Review verdict to each Task spec via `task_spec_section_write`, no further action; (b) minor → fix-in-place inline (Edit / Write) OR open deferred issue, then write §Code Review; (c) critical → apply fixes inline (Edit / Write), re-enter verify-loop ONCE; second critical → halt with `STAGE_CODE_REVIEW_CRITICAL_TWICE`. Triggers: \"/code-review {STAGE_ID}\", \"stage code review\", \"post-verify code review\"."
alwaysApply: false
---
Read and follow: @ia/skills/opus-code-review/SKILL.md

When this skill invokes MCP tools, follow its Tool recipe order exactly.
If this skill is lifecycle-heavy, check: @.cursor/rules/cursor-lifecycle-adapters.mdc
