---
description: "Sonnet pair-tail skill (seam #2). Reads §Stage File Plan tuple list emitted by stage-file-plan (Opus pair-head) from the master plan Stage block. For each tuple: runs reserve-id.sh, writes ia/backlog/{id}.yaml, writes ia/projects/{id}.md stub from project-spec-template, updates master-plan task-table row. After loop: runs materialize-backlog.sh once + validate:dead-project-specs + validate:backlog-yaml. Skips all Depends-on dep re-query (planner verified). Escalates on flock failure, anchor miss, or non-zero validator exit. Idempotent: re-run on partial state = zero diff. Triggers: \"stage-file-apply\", \"/stage-file-apply {ORCHESTRATOR_SPEC} {STAGE_ID}\", \"apply stage file plan\", \"pair-tail stage file\", \"materialize stage tuples\". Argument order (explicit): ORCHESTRATOR_SPEC first, STAGE_ID second."
alwaysApply: false
---
Read and follow: @ia/skills/stage-file-apply/SKILL.md

When this skill invokes MCP tools, follow its Tool recipe order exactly.
If this skill is lifecycle-heavy, check: @.cursor/rules/cursor-lifecycle-adapters.mdc
