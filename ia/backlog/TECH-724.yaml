id: TECH-724
type: TECH
title: curate.py log-reject --reason → rejected.jsonl
priority: high
status: open
section: Stage 6.5 — Curation-trained quality gate
spec: ia/projects/TECH-724.md
files:
  - tools/sprite-gen/src/curate.py
notes: |
  `tools/sprite-gen/src/curate.py` — new `log-reject <variant> --reason <tag>` subcommand appending to `curation/rejected.jsonl`. Controlled reason vocabulary: `roof-too-shallow`, `roof-too-tall`, `facade-too-saturated`, `ground-too-uniform`. Verb disambiguates from existing `reject` (TECH-179 glob-delete).
acceptance: |
  - [ ] `log-reject <variant> --reason <tag>` appends JSONL row
  - [ ] Row shape mirrors `promoted.jsonl` plus `reason: <tag>`
  - [ ] Invalid `<tag>` → CLI error (controlled vocab enforced)
  - [ ] Unit test covers all four initial reasons + invalid-reason error
depends_on:
  - TECH-723
depends_on_raw: TECH-723 — log-promote subcommand ships row-writer helpers first
related:
  - TECH-725
created: 2026-04-23
raw_markdown: |
  TECH-724 — curate.py log-reject --reason → rejected.jsonl
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.5.2
