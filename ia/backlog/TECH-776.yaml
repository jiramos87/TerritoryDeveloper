id: TECH-776
type: tech
title: "Fix mechanicalization-preflight regex for plan-digest rich format"
priority: medium
status: open
section: "Technical Debt"
spec: "ia/projects/TECH-776.md"
files:
  - "tools/mcp-ia-server/src/tools/mechanicalization-preflight-lint.ts"
  - "ia/rules/mechanicalization-contract.md"
  - "ia/skills/plan-digest/SKILL.md"
notes: |
  Preflight regex at mechanicalization-preflight-lint.ts:116 assumes pair-head key format
  (`file_path:` / `target_file:` / `pick:`). plan-digest rich format emits paths inside markdown
  code backticks on `— **operation**: create` lines — regex never matches, preflight flags
  `picks` field empty despite every edit carrying anchored path. Forces advisory escape hatch
  in plan-digest Phase 5 (grid-asset Stage 3.3 incident). Structural fix: extend regex to
  accept rich-format path anchors OR add distinct artifact-kind regex branch for `plan_digest`.
acceptance: |
  - mechanicalization_preflight_lint returns pass=true on valid rich-format §Plan Digest artifacts.
  - No false picks-empty findings when every edit tuple carries markdown-wrapped path anchor.
  - Existing pair-head format (key-style) still passes (regression-free).
  - Advisory escape hatch in ia/skills/plan-digest/SKILL.md Phase 5 step 4 can be removed after fix lands.
  - validate:all green.
depends_on: []
depends_on_raw: ""
related:
  - "TECH-772"
  - "TECH-773"
  - "TECH-774"
  - "TECH-775"
created: 2026-04-24
raw_markdown: |
  - [ ] **TECH-776** — Fix mechanicalization-preflight regex for plan-digest rich format
    - Type: tech
    - Priority: medium
    - Notes: Preflight regex assumes key-style pair-head format; rich-format plan-digest paths live in markdown code + `— **operation**:` lines. Extend regex or add artifact-kind branch. Remove advisory escape hatch in plan-digest Phase 5 once fixed.
