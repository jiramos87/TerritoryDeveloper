id: TECH-357
type: infrastructure / CI
title: Wire `materialize-concurrent` into validate chain (Stage 2.1 Phase 2)
priority: medium
status: open
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-357.md
files:
  - package.json
  - ARCHITECTURE.md
notes: |
  Add `validate:materialize-concurrent` script to root `package.json` running `tools/scripts/test/materialize-concurrent.sh`; chain into `validate:all` OR dedicated `validate:concurrency` sub-chain — match existing convention for `reserve-id-concurrent` (grep `package.json` for prior wiring and mirror). Document new script in `ARCHITECTURE.md` Local verification table if `reserve-id-concurrent` listed there; otherwise skip doc edit + note absence in §10 Lessons Learned. CI runs same chain.
acceptance: |
  `validate:materialize-concurrent` script in root `package.json`; wired into `validate:all` (or dedicated sub-chain matching `reserve-id-concurrent` convention); `ARCHITECTURE.md` Local verification table updated if precedent exists; `npm run validate:all` green locally.
depends_on:
  - TECH-356
depends_on_raw: TECH-356 (test script must exist to wire — hard gate)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-357** — Wire `materialize-concurrent` into validate chain (Stage 2.1 Phase 2)
    - Type: infrastructure / CI
    - Files: `package.json`, `ARCHITECTURE.md`
    - Spec: `ia/projects/TECH-357.md`
    - Notes: `validate:materialize-concurrent` npm script → `test/materialize-concurrent.sh`; chain into `validate:all` or sub-chain matching `reserve-id-concurrent` convention; update `ARCHITECTURE.md` Local verification table if precedent.
    - Acceptance: npm script present; wired per convention; ARCHITECTURE doc updated if precedent; `validate:all` green.
    - Depends on: TECH-356 (test script — hard gate)
    - Related: TECH-355, TECH-356
parent_plan: ia/projects/backlog-yaml-mcp-alignment-master-plan.md
task_key: T2.1.4
