id: TECH-701
type: TECH
title: Formalize pivot_pad patch + DAS-cited comment at compose.py:256
priority: high
status: open
section: Stage 6.1 — Pivot hotfix + regression tighten
spec: ia/projects/TECH-701.md
files:
  - tools/sprite-gen/src/compose.py
notes: |
  Retroactive filing. In-session hotfix `pivot_pad = 17 if spec.get("ground") != "none" else 0`; `adjusted_y0 = y0 - pivot_pad - offset_z` already applied at `tools/sprite-gen/src/compose.py:256`. Task locks the wording + DAS §2.1/§2.2 citation in the inline comment so the invariant does not drift. No code path change.
acceptance: |
  - [ ] `pivot_pad = 17 if spec.get("ground") != "none" else 0` confirmed at `compose.py:256`
  - [ ] `adjusted_y0 = y0 - pivot_pad - offset_z` confirmed
  - [ ] Inline comment names DAS §2.1 + §2.2 explicitly
  - [ ] `pytest tools/sprite-gen/tests -q` green (218+ tests)
depends_on: []
depends_on_raw: None — Stage 6.1 independent hotfix filing
related:
  - TECH-702
  - TECH-703
created: 2026-04-23
raw_markdown: |
  TECH-701 — Formalize pivot_pad patch + DAS-cited comment at compose.py:256
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.1.1
