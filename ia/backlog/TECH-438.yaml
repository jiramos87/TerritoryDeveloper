id: TECH-438
type: mcp tooling / filter extension
title: "Extend `backlog_list` inputs with locator filters `parent_plan` / `stage` / `task_key` (Stage 4.2 Phase 1)"
priority: medium
status: open
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-438.md
files:
  - tools/mcp-ia-server/src/tools/backlog-list.ts
notes: |
  In `tools/mcp-ia-server/src/tools/backlog-list.ts`, add optional input fields `parent_plan?`, `stage?`, `task_key?`. Apply filters in-memory AFTER existing `section` / `priority` / `type` / `status` / `scope` filters. Lowercase substring compare per N3 (`docs/parent-plan-locator-fields-exploration.md` §Phase 8) — matches existing filter pattern. Preserve id-desc ordering. Update tool descriptor JSON schema + use-case prose. Source fields come from schema-v2 yaml (Step 3 shipped). No behavior change for callers omitting the new inputs.
acceptance: |
  `backlog_list` accepts `parent_plan?` / `stage?` / `task_key?` inputs; filters applied in-memory after existing filters; lowercase substring compare; id-desc ordering preserved; tool descriptor updated; `validate:all` green.
depends_on:
  - TECH-363
  - TECH-364
depends_on_raw: "TECH-363 (schema v2 fields on ParsedBacklogIssue), TECH-364 (yaml loader maps locator fields)"
related:
  - TECH-413
  - TECH-415
  - TECH-439
created: 2026-04-18
parent_plan: ia/projects/backlog-yaml-mcp-alignment-master-plan.md
task_key: T4.2.1
step: 4
stage: "4.2"
router_domain: Backlog / issues
surfaces:
  - tools/mcp-ia-server/src/tools/backlog-list.ts
  - tools/mcp-ia-server/src/parser/backlog-yaml-loader.ts
  - docs/parent-plan-locator-fields-exploration.md
mcp_slices: []
skill_hints: []
raw_markdown: |
  - [ ] **TECH-438** — Extend `backlog_list` inputs with locator filters (Stage 4.2 Phase 1)
    - Type: mcp tooling / filter extension
    - Files: `tools/mcp-ia-server/src/tools/backlog-list.ts`
    - Spec: `ia/projects/TECH-438.md`
    - Notes: Add optional `parent_plan?` / `stage?` / `task_key?` inputs. In-memory lowercase substring compare. Applied after existing filters. id-desc ordering preserved. Descriptor updated.
    - Acceptance: new filters wired; descriptor updated; `validate:all` green.
    - Depends on: **TECH-363**, **TECH-364**
    - Related: TECH-413, TECH-415, TECH-439
