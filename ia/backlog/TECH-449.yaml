id: TECH-449
type: tech (IA infrastructure / lifecycle tooling)
title: Update glossary — 8 new terms + tombstones + flip M1 done (Stage 1.2 Phase 3)
priority: medium
status: open
section: IA evolution lane
spec: ia/projects/TECH-449.md
files:
  - ia/specs/glossary.md
  - ia/state/lifecycle-refactor-migration.json
notes: |
  Edit `ia/specs/glossary.md`: add 8 new rows — **Plan-Apply pair** (pair pattern: Opus writes structured plan into `§Plan` section; Sonnet applies), **plan review** (Opus stage reads all Stage Tasks + master-plan header + invariants; outputs `§Plan Fix`), **plan-fix apply** (Sonnet pair-tail reads `§Plan Fix` + applies edits), **spec enrichment** (Sonnet stage pulls glossary anchors + tightens spec terminology; replaces kickoff), **Opus audit** (Opus post-verify stage; reads spec→impl→findings→verify; writes `§Audit` + `§Closeout Plan`), **Opus code review** (Opus stage reads diff vs spec + invariants + glossary; PASS / minor / `§Code Fix Plan`), **code-fix apply** (Sonnet pair-tail reads `§Code Fix Plan` + applies + re-enters `/verify-loop`), **closeout apply** (Sonnet pair-tail reads `§Closeout Plan` + archives BACKLOG row + deletes spec + validates). Redefine **Stage** as parent-of-Task. Redefine **Project hierarchy** to 2-level (Stage → Task). Tombstone **Phase** (redirect: use Stage) + **Gate** (redirect: use Stage exit criteria). Run `npm run validate:frontmatter`. Flip M1 done in `ia/state/lifecycle-refactor-migration.json`.
acceptance: |
  `ia/specs/glossary.md` carries 8 new rows; **Stage** + **Project hierarchy** redefined per Stage 1.2 Exit; **Phase** + **Gate** tombstoned w/ redirect to **Stage**; `ia/state/lifecycle-refactor-migration.json` M1 = `done`; `npm run validate:frontmatter` exit 0; `npm run validate:all` exit 0.
depends_on:
  - TECH-444
  - TECH-445
  - TECH-446
  - TECH-447
  - TECH-448
depends_on_raw: TECH-444 + TECH-445 + TECH-446 + TECH-447 + TECH-448 (all foundation surfaces must be in final shape before glossary rows reference them; M1 flip is the Stage 1.2 close gate).
related: []
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T1.2.6
raw_markdown: |
  - [ ] **TECH-449** — Update glossary + flip M1 done (Stage 1.2 Phase 3)
    - Type: tech (IA infrastructure / lifecycle tooling)
    - Files: `ia/specs/glossary.md`, `ia/state/lifecycle-refactor-migration.json`
    - Spec: `ia/projects/TECH-449.md`
    - Notes: Add 8 new glossary rows; redefine Stage + Project hierarchy; tombstone Phase + Gate; flip migration JSON M1 `done`.
    - Acceptance: 8 rows added; redefinitions present; tombstones present; M1 done; `validate:all` green.
    - Depends on: **TECH-444**, **TECH-445**, **TECH-446**, **TECH-447**, **TECH-448**
    - Related: none
