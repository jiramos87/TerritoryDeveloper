id: TECH-710
type: TECH
title: variants block + split seeds loader normalization
priority: high
status: open
section: Stage 6.3 — Placement + variant randomness + split seeds
spec: ia/projects/TECH-710.md
files:
  - tools/sprite-gen/src/spec.py
notes: |
  `tools/sprite-gen/src/spec.py` accepts `variants: {count, vary, seed_scope}` object; legacy scalar `variants: N` normalises to `{count: N, vary: {}, seed_scope: palette}`. Top-level `palette_seed: int` + `geometry_seed: int`; legacy scalar `seed: N` fans to both when split seeds absent. `seed_scope` default `palette` preserves legacy behaviour. Consumes L6, L14.
acceptance: |
  - [ ] Scalar `variants: N` normalises to `{count: N, vary: {}, seed_scope: palette}`
  - [ ] Object `variants: {count, vary, seed_scope}` surfaced as-is
  - [ ] `seed: N` fans to `palette_seed = geometry_seed = N` when split seeds absent
  - [ ] Explicit split seeds are preserved
  - [ ] Unit tests for each legacy / object shape
depends_on:
  - TECH-704
  - TECH-705
  - TECH-706
  - TECH-707
  - TECH-708
depends_on_raw: Stage 6.2 merged
related:
  - TECH-709
  - TECH-711
created: 2026-04-23
raw_markdown: |
  TECH-710 — variants block + split seeds loader normalization
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.3.2
