id: TECH-355
type: infrastructure / scripting
title: Flock-guard `materialize-backlog.sh` + self-documenting header (Stage 2.1 Phase 1)
priority: medium
status: open
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-355.md
files:
  - tools/scripts/materialize-backlog.sh
notes: |
  Wrap `node tools/scripts/materialize-backlog.mjs …` call inside `tools/scripts/materialize-backlog.sh` w/ `flock ia/state/.materialize-backlog.lock` (per invariant #13 lockfile-per-domain rule — orchestrator prose says `.backlog.lock`, invariant says `.materialize-backlog.lock`; use invariant name). Create lock file if absent (touch under flock trap; same pattern as `reserve-id.sh`). Add caveman header comment naming lock path + rationale ("parallel stage-file + MCP `backlog_record_create` writers serialize here") + cross-ref to `reserve-id.sh` for flock pattern. Merges original T2.1.1 (flock wrap) + T2.1.2 (header doc) — Decision Log: flock wrap + self-documenting header = atomic edit on same file; split creates thrash. No behavior change for single-writer callers.
acceptance: |
  `tools/scripts/materialize-backlog.sh` routes `.mjs` invocation through `flock ia/state/.materialize-backlog.lock`; lock file auto-created if missing; header comment names lock path + rationale + cross-refs `reserve-id.sh`; `npm run validate:all` green; single-writer callers unchanged.
depends_on: []
depends_on_raw: ""
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-355** — Flock-guard `materialize-backlog.sh` + self-documenting header (Stage 2.1 Phase 1)
    - Type: infrastructure / scripting
    - Files: `tools/scripts/materialize-backlog.sh`
    - Spec: `ia/projects/TECH-355.md`
    - Notes: Wrap `.mjs` invocation w/ `flock ia/state/.materialize-backlog.lock` (invariant #13 lockfile-per-domain). Lock file auto-touch under trap. Caveman header comment documents lock path + rationale + `reserve-id.sh` cross-ref. Merges T2.1.1 + T2.1.2 per Decision Log.
    - Acceptance: flock routes `.mjs` call; lock auto-created; header documents; `validate:all` green; single-writer unchanged.
    - Related: TECH-356, TECH-357
parent_plan: ia/projects/backlog-yaml-mcp-alignment-master-plan.md
task_key: T2.1.1
