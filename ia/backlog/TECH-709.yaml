id: TECH-709
type: TECH
title: Placement schema — building.footprint_px / padding / align
priority: high
status: open
section: Stage 6.3 — Placement + variant randomness + split seeds
spec: ia/projects/TECH-709.md
files:
  - tools/sprite-gen/src/spec.py
notes: |
  `tools/sprite-gen/src/spec.py` accepts `building.footprint_px: [bx, by]` (wins over `footprint_ratio`), `building.padding: {n, e, s, w}` in px (default all 0), `building.align ∈ {center, sw, ne, nw, se, custom}` (default `center`). Back-compat: existing specs without these fields render byte-identical. Consumes L5.
acceptance: |
  - [ ] `load_spec` surfaces `building.footprint_px` when present
  - [ ] `load_spec` surfaces `building.padding` (default `{n:0,e:0,s:0,w:0}`)
  - [ ] `load_spec` surfaces `building.align` (default `center`)
  - [ ] Existing specs unchanged; render byte-identical
  - [ ] Unit tests for each field
depends_on:
  - TECH-704
  - TECH-705
  - TECH-706
  - TECH-707
  - TECH-708
depends_on_raw: Stage 6.2 merged (signature module + CLI + loader flag + tests + DAS pointer)
related:
  - TECH-710
  - TECH-711
created: 2026-04-23
raw_markdown: |
  TECH-709 — Placement schema — building.footprint_px / padding / align
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.3.1
