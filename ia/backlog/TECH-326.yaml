id: TECH-326
type: infrastructure / MCP tooling
title: Implement `reserve_backlog_ids` MCP tool (Stage 1.2 Phase 2)
priority: high
status: open
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-326.md
files:
  - tools/mcp-ia-server/src/tools/reserve-backlog-ids.ts
  - tools/mcp-ia-server/src/index.ts
notes: |
  MCP tool `reserve_backlog_ids` — input `{ prefix: "TECH"|"FEAT"|"BUG"|"ART"|"AUDIO", count: 1..50 }`, spawn `tools/scripts/reserve-id.sh {prefix} {count}` via `child_process`, parse stdout, return `{ ids: string[] }`. Wraps monotonic id source per invariant #13 — never touches `ia/state/id-counter.json` directly. Stage-shared context: Phase 2 of Stage 1.2 ships the id-reservation surface consumed by `stage-file` / `project-new` skills + IP6 `backlog_record_create`.
acceptance: |
  Tool registered; single + batch invocations return correct ids; counter advances by `count`; input validation rejects invalid prefix / count > 50; happy-path test green.
depends_on: []
depends_on_raw: none
related:
  - TECH-327
  - TECH-324
created: 2026-04-17
raw_markdown: |
  - [ ] **TECH-326** — Implement `reserve_backlog_ids` MCP tool (Stage 1.2 Phase 2)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/src/tools/reserve-backlog-ids.ts` (new), `tools/mcp-ia-server/src/index.ts`
    - Spec: `ia/projects/TECH-326.md`
    - Notes: Spawn `tools/scripts/reserve-id.sh {prefix} {count}` via `child_process`; parse stdout; return `{ ids: string[] }`. Invariant #13 — script-backed, no direct counter edits.
    - Acceptance: tool registered; single + batch work; counter advances; input validation; happy-path test green.
    - Related: TECH-327, TECH-324
