id: TECH-766
type: TECH
title: "`iso_path` + `iso_pavement_patch` primitives"
priority: medium
status: open
section: "Stage 7 — Decoration primitives — vegetation & yard"
spec_path: ia/projects/TECH-766.md
files: []
notes: |
  Two yard-surface primitives colocated. `iso_path` — beige/grey walkway strip; `axis ∈ {ns, ew}`; width 2–4 px. `iso_pavement_patch` — fills arbitrary rect. Both under palette key `pavement`; no outline pass.
depends_on: []
depends_on_raw: ""
related:
  - TECH-767
  - TECH-770
acceptance: |
  - [ ] `iso_path(canvas, x0, y0, length_px, axis, palette, width_px=2)` draws strip; `axis ∈ {ns, ew}`; `width_px ∈ [2, 4]`.
  - [ ] `iso_pavement_patch(canvas, x0, y0, w_px, d_px, palette)` fills rect with beige/grey under palette key `pavement`.
  - [ ] Invalid axis or width raises `ValueError` with canonical list in message.
created: 2026-04-24
raw_markdown: |
  - [ ] **TECH-766** — `iso_path` + `iso_pavement_patch` primitives
    - Type: TECH
    - Priority: medium
    - Files: `tools/sprite-gen/src/primitives/iso_path.py`, `iso_pavement_patch.py`
    - Related: **TECH-767**, **TECH-770**
    - Notes: Two yard-surface primitives colocated. `iso_path` — beige/grey walkway strip; `axis ∈ {ns, ew}`; width 2–4 px. `iso_pavement_patch` — fills arbitrary rect. Both under palette key `pavement`; no outline pass.
