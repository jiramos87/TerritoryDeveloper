id: TECH-704
type: TECH
title: Signature module core (src/signature.py) with L15 sample-size policy
priority: high
status: open
section: Stage 6.2 — Art Signatures per class
spec: ia/projects/TECH-704.md
files:
  - tools/sprite-gen/src/signature.py
notes: |
  New module `tools/sprite-gen/src/signature.py` exposing `compute_signature(class_name, folder_glob) -> dict`, `validate_against(signature, rendered_img) -> ValidationReport`, and `SignatureStaleError`. JSON shape per handoff §3 Stage 6.2 (class / refreshed_at / source_count / source_checksum / mode / fallback_of / bbox / palette / silhouette / ground / decoration_hints). L15 branches: source_count 0 → fallback, 1 → point-match, ≥2 → envelope. L3 staleness guard: recompute checksum on validate; mismatch raises `SignatureStaleError("signature stale — run python3 -m src refresh-signatures <class>")`.
acceptance: |
  - [ ] `compute_signature(class_name, folder_glob)` returns documented JSON dict
  - [ ] `validate_against(signature, rendered_img)` returns `ValidationReport(ok, failures)`
  - [ ] L15 sample-size policy implemented: 0 → fallback, 1 → point-match, ≥2 → envelope
  - [ ] L3 staleness guard: checksum mismatch raises `SignatureStaleError`
  - [ ] Unit tests green: `pytest tools/sprite-gen/tests/test_signature.py -q`
depends_on:
  - TECH-701
  - TECH-702
  - TECH-703
depends_on_raw: Stage 6.1 merged (pivot hotfix locked + regression tightened)
related: []
created: 2026-04-23
raw_markdown: |
  TECH-704 — Signature module core (src/signature.py) with L15 sample-size policy
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.2.1
