id: TECH-741
type: TECH
title: Slot name grammar — tiled-(row|column)-N parser
priority: high
status: open
section: Stage 9 addendum — Parametric tiled-row-N / tiled-column-N
spec: ia/projects/TECH-741.md
files:
  - tools/sprite-gen/src/slots.py
notes: |
  `tools/sprite-gen/src/slots.py` — parse slot name via regex `^tiled-(row|column)-(\d+)$`; capture axis + `N`; validate `N ≥ 2`; otherwise raise `SpecError`. Hard-coded legacy names (`tiled-row-3`, etc.) alias through parser.
acceptance: |
  - [ ] Regex parse captures axis ∈ {row, column} and `N`
  - [ ] `N < 2` or non-int raises `SpecError` with offending name
  - [ ] Hard-coded legacy names still accepted transitionally
  - [ ] Unit tests cover accept + reject paths (in TECH-743)
depends_on: []
depends_on_raw: Standalone parser — no dependency
related:
  - TECH-742
created: 2026-04-23
raw_markdown: |
  TECH-741 — Slot name grammar — tiled-(row|column)-N parser
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T9.2.1
