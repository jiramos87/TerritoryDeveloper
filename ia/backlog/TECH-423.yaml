id: TECH-423
type: tech / save migration
title: "`MigrateLoadedSaveData` v3→v4 branch (Stage 1.3 Phase 3)"
priority: medium
status: open
section: Medium Priority
spec: ia/projects/TECH-423-migrate-save-v3-to-v4.md
files:
  - Assets/Scripts/Managers/GameManagers/GameSaveManager.cs
notes: |
  Add `if (data.schemaVersion < 4)` branch in `MigrateLoadedSaveData` (`GameSaveManager.cs`): `data.stateServiceZones = new List<StateServiceZoneData>(); data.budgetAllocation = BudgetAllocationData.Default(cap: ${derive-from-treasury-snapshot-or-constant}); data.schemaVersion = 4;`. Cap-derivation choice documented in Decision Log (prefer constant like `10_000` for MVP determinism). Seeds equal 14.28% × 7 envelope normalized to 1.0 + empty S zone list. Legacy v3 saves load cleanly; envelope remaining pre-seeded.
acceptance: |
  v3 save loads → v4 post-migration w/ non-null `budgetAllocation` (sum == 1.0) + empty `stateServiceZones`. Existing v3 data preserved byte-identical outside new fields. `unity:compile-check` green. Migration test coverage in TECH-425.
depends_on:
  - TECH-422
depends_on_raw: "**TECH-422** (Phase 3 schema bump — new types consumed here)"
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-423** — `MigrateLoadedSaveData` v3→v4 branch (Stage 1.3 Phase 3)
    - Type: tech / save migration
    - Files: `Assets/Scripts/Managers/GameManagers/GameSaveManager.cs`
    - Spec: `ia/projects/TECH-423-migrate-save-v3-to-v4.md`
    - Notes: Migration branch seeds equal 14.28% × 7 envelope + empty S list on v3→v4 load. Cap constant documented in Decision Log.
    - Acceptance: v3 save round-trips into v4; existing data preserved; `unity:compile-check` green.
    - Depends on: **TECH-422**
parent_plan: ia/projects/zone-s-economy-master-plan.md
task_key: T1.3.6
