id: TECH-822
type: TECH
title: Imp-8 — Idempotency + dry-run mode for bake handler
priority: P3
status: pending
section: UI bake pipeline
files:
  - Assets/Scripts/Editor/Bridge/UiBakeHandler.cs
notes: |
  SavePanelSnapshotPrefab(item, options { dry_run, force_overwrite }).
  Dry-run → skip SaveAsPrefabAsset, return BakeReport only.
  Default mode → diff destination components vs expected; warn on unexpected components.
  Prevents silent destruction of user manual edits on re-bake.
acceptance:
  - BakeOptions { dry_run, force_overwrite } param added to SavePanelSnapshotPrefab
  - Dry-run path skips SaveAsPrefabAsset + returns BakeReport
  - Default path diffs destination; warns on unexpected components
  - Compile-check green
depends_on: [TECH-819]
depends_on_raw: TECH-819
related: []
created: '2026-05-08'
