id: TECH-329
type: infrastructure / MCP tooling
title: Test `backlog_list` filter combinations (Stage 1.2 Phase 3)
priority: high
status: open
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-329.md
files:
  - tools/mcp-ia-server/tests/tools/backlog-list.test.ts
notes: |
  Fixture set for `backlog_list` covering ≥2 sections, ≥2 priorities, ≥2 types, open + archive records. Assertions: scope switch, single-filter cases, multi-filter intersection, empty result, id-desc ordering. Locks filter semantics before `backlog_search` filter extensions in Stage 2.3. Stage-shared context: final task of Stage 1.2; post-landing all three MCP tools (reserve / list / validate) are ready for skill-doc wiring in Stage 1.3.
acceptance: |
  Test file covers each filter dim + combos + scope switch + empty + ordering; `npm run validate:all` green.
depends_on: [TECH-328]
depends_on_raw: "TECH-328 (list tool must exist to test — hard gate)"
related:
  - TECH-328
created: 2026-04-17
raw_markdown: |
  - [ ] **TECH-329** — Test `backlog_list` filter combinations (Stage 1.2 Phase 3)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/tests/tools/backlog-list.test.ts` (new)
    - Spec: `ia/projects/TECH-329.md`
    - Notes: Fixture set covering ≥2 sections, ≥2 priorities, ≥2 types, open + archive. Assert scope switch, single-filter cases, multi-filter intersection, empty result, id-desc ordering.
    - Acceptance: each filter dim + combos + scope + empty + ordering covered; `validate:all` green.
    - Depends on: TECH-328 (list tool — hard gate)
    - Related: TECH-328
