id: TECH-713
type: TECH
title: Tests — placement + variants + split seeds
priority: high
status: open
section: Stage 6.3 — Placement + variant randomness + split seeds
spec: ia/projects/TECH-713.md
files:
  - tools/sprite-gen/tests/test_building_placement.py
  - tools/sprite-gen/tests/test_variants_geometric.py
  - tools/sprite-gen/tests/test_split_seeds.py
notes: |
  Three new test files. `test_building_placement.py` — matrix over footprint_px / ratio / padding / align; asserts resolved mass bbox per case. `test_variants_geometric.py` — same spec + `vary:` produces 4 variants with pairwise-distinct bboxes; identical outputs across runs with same seeds. `test_split_seeds.py` — freezing `palette_seed` varies only geometry when `geometry_seed` advances, and vice versa.
acceptance: |
  - [ ] `test_building_placement.py` ≥12 matrix cases pass
  - [ ] `test_variants_geometric.py` 4 variants pairwise-distinct + reproducible
  - [ ] `test_split_seeds.py` asserts seed-split independence in both directions
  - [ ] `pytest tools/sprite-gen/tests/ -q` green
depends_on:
  - TECH-711
  - TECH-712
depends_on_raw: TECH-711 + TECH-712 — helper + CLI must land first
related: []
created: 2026-04-23
raw_markdown: |
  TECH-713 — Tests — placement + variants + split seeds
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.3.5
