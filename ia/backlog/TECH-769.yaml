id: TECH-769
type: TECH
title: Composer `decorations:` integration
priority: high
status: open
section: "Stage 7 — Decoration primitives — vegetation & yard"
spec_path: ia/projects/TECH-769.md
files: []
notes: |
  `tools/sprite-gen/src/compose.py` — `compose_sprite` reads `spec.decorations`; calls `placement.place(...)`; draws z-order ground → yard-deco → building → roof-deco. Raises `DecorationScopeError` on 1×1 footprint + `iso_pool`.
depends_on:
  - TECH-768
depends_on_raw: "**TECH-768**"
related:
  - TECH-765
  - TECH-771
acceptance: |
  - [ ] `compose_sprite` reads `spec.decorations: list[...]`; iterates `placement.place(...)` output.
  - [ ] Z-order enforced: ground diamond → yard decorations → building → roof decorations.
  - [ ] Footprint-scope gate raises `DecorationScopeError` when a 1×1 spec includes `iso_pool`.
created: 2026-04-24
raw_markdown: |
  - [ ] **TECH-769** — Composer `decorations:` integration
    - Type: TECH
    - Priority: high
    - Files: `tools/sprite-gen/src/compose.py`
    - Depends on: **TECH-768**
    - Related: **TECH-765**, **TECH-771**
    - Notes: `compose_sprite` reads `spec.decorations`; calls `placement.place(...)`; draws z-order ground → yard-deco → building → roof-deco. Raises `DecorationScopeError` on 1×1 footprint + `iso_pool`.
