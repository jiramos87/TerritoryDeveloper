id: TECH-446
type: tech (IA infrastructure / lifecycle tooling)
title: Rewrite project-hierarchy rule — 2-level Stage·Task (Stage 1.2 Phase 2)
priority: medium
status: open
section: IA evolution lane
spec: ia/projects/TECH-446.md
files:
  - ia/rules/project-hierarchy.md
notes: |
  Rewrite `ia/rules/project-hierarchy.md` §table from 4-row (Step·Stage·Phase·Task) → 2-row (Stage·Task). Restate cardinality gate: ≥2 tasks per Stage (hard), ≤6 tasks per Stage (soft). Update lazy-materialization rule to Stage granularity (was Phase). Update ephemeral-spec rule: Tasks still get individual `ia/projects/{ISSUE_ID}.md` specs. Pure tooling.
acceptance: |
  `ia/rules/project-hierarchy.md` table = 2 rows (Stage · Task); cardinality gate = ≥2 tasks per Stage; Phase + Gate rows absent; lazy-materialization at Stage granularity; `npm run validate:frontmatter` exit 0; `npm run validate:all` exit 0.
depends_on:
  - TECH-444
  - TECH-445
depends_on_raw: TECH-444 + TECH-445 (templates updated first so rule prose can reference correct template anchors).
related:
  - TECH-447
  - TECH-448
  - TECH-449
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T1.2.3
raw_markdown: |
  - [ ] **TECH-446** — Rewrite project-hierarchy rule (Stage 1.2 Phase 2)
    - Type: tech (IA infrastructure / lifecycle tooling)
    - Files: `ia/rules/project-hierarchy.md`
    - Spec: `ia/projects/TECH-446.md`
    - Notes: Collapse 4-row table → 2-row (Stage·Task); cardinality gate ≥2 tasks per Stage; Phase + Gate rows removed.
    - Acceptance: 2-row table; gate restated; `validate:all` green.
    - Depends on: **TECH-444**, **TECH-445**
    - Related: TECH-447, TECH-448, TECH-449
