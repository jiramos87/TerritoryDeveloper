id: TECH-715
type: TECH
title: Ground schema — string / object form loader normalization
priority: high
status: open
section: Stage 6.4 — Ground variation
spec: ia/projects/TECH-715.md
files:
  - tools/sprite-gen/src/spec.py
notes: |
  `tools/sprite-gen/src/spec.py` — `ground:` accepts string (normalises to `{material: <str>}`, zero jitter, no texture) or full object (`material` OR `materials: [...]`, optional `hue_jitter: float`, `value_jitter: float`, `texture: {primitive, density, palette}`). Back-compat: string form stays byte-identical. Consumes L8.
acceptance: |
  - [ ] String `ground: grass_flat` normalises to `{material: "grass_flat"}`, zero jitter, no texture
  - [ ] Object form round-trips with defaults filled in
  - [ ] `materials: [...]` accepted as pool for variant sampling
  - [ ] Existing specs unchanged; render byte-identical
  - [ ] Unit tests cover both forms
depends_on:
  - TECH-709
  - TECH-710
  - TECH-711
  - TECH-712
  - TECH-713
  - TECH-714
depends_on_raw: Stage 6.3 merged (placement + variants + split seeds)
related:
  - TECH-718
  - TECH-720
created: 2026-04-23
raw_markdown: |
  TECH-715 — Ground schema — string / object form loader normalization
parent_plan: ia/projects/sprite-gen-master-plan.md
task_key: T6.4.1
