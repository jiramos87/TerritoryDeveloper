id: TECH-339
type: tech (web data layer)
title: Author `web/lib/releases.ts` — Release registry + resolver (Stage 7.1 T7.1.1)
priority: medium
status: open
section: Web platform lane
spec: ia/projects/TECH-339.md
files:
  - web/lib/releases.ts
notes: |
  Hand-maintained release registry. `Release` interface — `id`, `label`, `umbrellaMasterPlan`, `children: string[]`. `resolveRelease(id: string): Release | null` lookup by id. Seed `releases` const array w/ single `full-game-mvp` row (9 children from `docs/web-platform-post-mvp-extensions.md` §1 Examples block). Header comment cites `ia/projects/full-game-mvp-rollout-tracker.md` as source of truth for `children[]` — flag drift risk. No routes, no UI, no auth. Stage 7.1 Phase 1 of web-platform-master-plan.
acceptance: |
  `web/lib/releases.ts` exports `Release` interface + `resolveRelease` + seeded `releases` array; header comment cites rollout tracker doc; `npm run validate:web` green.
depends_on: []
depends_on_raw: none
related:
  - TECH-340
  - TECH-341
  - TECH-342
created: 2026-04-17
raw_markdown: |
  - [ ] **TECH-339** — Author `web/lib/releases.ts` — Release registry + resolver (Stage 7.1 T7.1.1)
    - Type: tech (web data layer)
    - Files: `web/lib/releases.ts` (new)
    - Spec: `ia/projects/TECH-339.md`
    - Notes: `Release` interface + `resolveRelease()` + seeded `full-game-mvp` row (9 children). Header cites `full-game-mvp-rollout-tracker.md`. Stage 7.1 Phase 1 of web-platform-master-plan.
    - Acceptance: registry compiles; `resolveRelease` returns seeded row + null on miss; `validate:web` green.
    - Related: TECH-340, TECH-341, TECH-342
