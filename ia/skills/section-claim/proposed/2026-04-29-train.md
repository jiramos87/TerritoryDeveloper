---
skill: section-claim
generated: 2026-04-29
friction_count: 0
threshold: 2
schema_version: 2026-04-29
---

# skill-train — section-claim — 2026-04-29

## Scan window

`--since 2026-04-22` (parallel-carcass-rollout birth; covers all section claim operations in Wave 1)

## Changelog entries scanned

| Date | Entry | Format |
|------|-------|--------|
| 2026-04-29 | NEW skill — PR 3.1 (DEC-A19 Phase E recipify). 0 seams: `git worktree add` + `mcp.section_claim` + sentinel write. Heartbeats external. | table-row |
| 2026-04-29 | V2 rewrite — dropped `session_id`, dropped git worktree step, dropped sentinel write. Same branch + same worktree model. Row-key mutex. | table-row |

## Friction aggregation

Focus areas per §Plan Digest: `concurrent_claim_race`, `heartbeat_sweep_noise`.

No `source: self-report` JSON entries found. Both entries are feature-milestones (new skill creation + V2 protocol rewrite).

Recurrence by friction_type:
- `concurrent_claim_race`: 0 (no race conditions observed during Wave 1 single-threaded dogfood)
- `heartbeat_sweep_noise`: 0 (claims sweep ran cleanly in Stage 2.3 cron sweep stage)

**Result: no self-report entries in scan window — nothing to aggregate.**

## Notes

- V2 row-key mutex is simpler and correct for sequential Wave 1 use case. No concurrent race stress-tested yet.
- Guardrail already covers concurrent INSERT (`section_claim_held` → retry). No additional write-back needed.
- Recommend: when stress-testing parallel sections in Wave 2, wire `source: self-report` emitter if `section_claim_held` fires more than once per session.

## Review + apply manually. Do NOT auto-apply.
