---
skill: ship-stage
generated: 2026-04-29
friction_count: 0
threshold: 2
schema_version: 2026-04-29
---

# skill-train — ship-stage — 2026-04-29

## Scan window

`--since 2026-04-22` (parallel-carcass-rollout birth; covers all staged shipped during Wave 1 dogfood)

## Changelog entries scanned

| Date | Entry | friction_types |
|------|-------|----------------|
| 2026-04-29 | parallel-carcass Wave 0 Phase 3 PR 3.5 — added `stage_claim` / `stage_claim_release` / `claim_heartbeat` / `arch_drift_scan` to tools_extra; documented Pass A/B carcass hooks (conditional on `stage.section_id`; legacy linear plans skip all hooks) | feature-extension |
| 2026-04-29 | parallel-carcass V2 rewrite — dropped `session_id` from all claim calls; switched sentinel check to `stage_bundle.stage.section_id`; pure row-key mutex | feature-extension |

## Friction aggregation

Focus areas per §Plan Digest: `pass_a_pre_claim`, `pass_b_drift_fp`, `heartbeat_noise`.

Neither Changelog entry maps to these friction types — both are feature-extensions (protocol upgrades). No `source: self-report` JSON entries found.

Recurrence by friction_type:
- `pass_a_pre_claim`: 0
- `pass_b_drift_fp`: 0
- `heartbeat_noise`: 0
- `feature-extension` (non-friction): 2 (below threshold + non-friction category)

**Result: no friction signals at threshold 2 — no diff proposed.**

## Notes

- V2 rewrite dropped session_id + sentinel elegantly. No observed friction during stages 2.1–2.4.
- Recommend adding `source: self-report` emitter stanza to Phase 9 tail (chain digest step) when friction conditions fire in future runs. Focus conditions: `stage_claim` held ≥30s, `arch_drift_scan` returns affected_stages, Pass B verify-loop iteration > 1.

## Review + apply manually. Do NOT auto-apply.
