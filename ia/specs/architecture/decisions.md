# Architecture decisions

Compact table-driven record of architectural decisions. Each row maps to an `arch_decisions` row (status=active until superseded). NO ADR-style multi-paragraph entries — locked exploration decision (`source-of-truth-split` + `decisions-doc-shape`).

| slug | title | status | rationale | alternatives | superseded_by |
|------|-------|--------|-----------|--------------|---------------|
| DEC-A1 | gridmanager-as-hub | active | Central coordinator for cell operations gives consistent access surface; large class size accepted as current trade-off (see DEC-A10). | decompose into N services; ECS; ServiceLocator | — |
| DEC-A2 | findobjectoftype-pattern | active | Inspector wiring + null-check fallback in Awake/Start avoids DI framework overhead and matches Unity-native conventions. | constructor DI; ServiceLocator; ECS | — |
| DEC-A3 | namespaces-territory-prefix | active | Most code under `Territory.*` (Core, Terrain, Roads, Zones, Forests, Buildings, Economy, UI, Geography, Timing, Utilities, Simulation, Persistence). Few legacy in global namespace. | flat namespace; per-feature root | — |
| DEC-A4 | spec-policy-agents-md | active | `AGENTS.md` carries spec policy; spec inventory under `ia/specs/`; agent routing via `ia/rules/agent-router.md`. Optional MCP via `docs/mcp-ia-server.md`; generic pattern via `docs/mcp-markdown-ia-pattern.md`. | inline policy in CLAUDE.md; per-spec headers | — |
| DEC-A5 | editor-agent-diagnostics-ia | active | `Assets/Scripts/Editor/AgentDiagnosticsReportsMenu.cs` emits JSON/Markdown under `tools/reports/` (gitignored outputs). Field vocabulary in `ia/specs/unity-development-context.md` §10; regressions tracked via BACKLOG. | runtime telemetry; manual exports | — |
| DEC-A6 | ide-agent-bridge-postgres | active | `unity_bridge_command`, `unity_bridge_get`, `unity_compile` enqueue work for `AgentBridgeCommandRunner` via `agent_bridge_job`. Bridge `kind` values include `get_compilation_status`. Play Mode smoke sequence reduces manual Play/Stop clicks. | filesystem queue; named pipes; HTTP service | — |
| DEC-A7 | high-coupling-accepted | active | Managers reference each other directly. Trade-off: simpler Inspector wiring vs harder unit isolation. | event bus; mediator pattern | — |
| DEC-A8 | gridmanager-size-accepted | active | GridManager ~2070 lines; decomposition tracked in BACKLOG. Trade-off: hub clarity vs single-class growth. | split per concern (cell ops / dispatch / queries) | — |
| DEC-A9 | no-event-system | active | Direct method calls between managers. Trade-off: explicit data flow vs subscriber decoupling. | C# events; UniRx; SignalBus | — |
| DEC-A10 | source-of-truth-split | active | Doc-primary, DB-indexed: humans edit markdown, DB indexes relations. Keeps git-blame on architectural prose; SQL serves agent queries. | DB-primary (markdown rendered from rows); doc-only (no DB); dual-write hard sync | — |
| DEC-A11 | doc-home-architecture-subdir | active | `ia/specs/architecture/{layers,data-flows,interchange,decisions}.md` is first-class IA. Permanent domain under `ia/specs/` per Invariant #12. | root `ARCHITECTURE.md`; `ia/projects/`; `docs/architecture/` | — |
| DEC-A12 | plan-arch-link-stage-level | active | Stage-level `arch_surfaces[]` field via `stage_arch_surfaces` link table. Joins on `arch_surfaces.slug`; clean SQL queries. | task-level link; JSONB column on `ia_stages`; tag string array | — |
| DEC-A13 | drift-trigger-on-write-plus-on-demand | active | Drift scan fires on arch decision write + on-demand `/arch-drift-scan`. NO pre-stage-file gate (avoids friction during planning). | pre-stage-file gate; nightly cron only; manual-only | — |
| DEC-A14 | drift-reaction-poll-never-rewrite | active | Drift report + relentless human polling per affected Stage. Never auto-rewrite plans — humans decide rationale. | auto-rewrite Stage scope; silent log; abort planning until resolved | — |
| DEC-A15 | arch-authoring-via-design-explore | active | New `Architecture Decision` phase inside `/design-explore` between `Select Approach` and `Expand`. Avoids new top-level command for arch authoring. | new `/arch-decide` command; manual SQL inserts; spec-only edits | — |
| DEC-A16 | db-relations-four-tables | active | All four tables: `arch_surfaces`, `arch_decisions`, `arch_changelog`, `stage_arch_surfaces`. Full coverage of surface inventory + decision rows + append-only history + plan↔arch links. | drop changelog (append to plan change_log instead); merge surfaces+decisions; single JSONB blob | — |
| DEC-A17 | decisions-doc-shape-table-driven | active | `decisions.md` = compact table-driven; NO ADR-style multi-paragraph records. Rationale ≤250 chars per row; alternatives ≤3 semicolon-separated. | ADR per decision; YAML frontmatter; long-form prose | — |
| DEC-A18 | db-lifecycle-extensions-2026-04-28 | active | DB-architecture leverage points compound: each shaves agent token cost + speeds skill chain. Lock now to amortize migration churn into one window. | wait-for-friction; mechanical-fillers-now-arch-drift-later; targeted-subset-only | — |
| DEC-A19 | agent-recipe-runner-2026-04-28 | active | Two-layer model: deterministic recipe-engine + narrow LLM seams. Compounds on DEC-A18 db-lifecycle primitives. Token cost ↓~60%, drift gate clean, plan-covered via subagent dispatch. | A1 status-quo+docs; A2 seams-only; A3 YAML-engine flow-only; A4 TS-modules+SDK-direct (loses plan billing) | — |
| DEC-A20 | game-ui-standalone-not-asset-pipeline | active | `game-ui-design-system` flows CD bundle → IR JSON → Unity bridge bake (pure agent-skill). NOT routed through asset-pipeline (web console → DB → catalog → fixtures). Plan-level waiver from `arch_surfaces` linkage; local save-schema extensions in-scope. | route via asset-pipeline catalog; per-stage backfill; per-run override | — |
