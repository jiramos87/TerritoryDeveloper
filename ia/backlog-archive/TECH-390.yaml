id: TECH-390
type: mcp / tests
title: "**MCP** — envelope wrapTool unit tests (Stage 2.1 T2.1.3)"
priority: medium
status: open
section: IA infrastructure
spec: ia/projects/TECH-390.md
files:
  - tools/mcp-ia-server/tests/envelope.test.ts
notes: |
  Unit tests in `tools/mcp-ia-server/tests/envelope.test.ts`: `wrapTool` happy path (`ok: true, payload`); envelope passthrough (handler returns already-shaped envelope — no double-wrap); bare `Error` throw → `ok: false, error.code = "internal_error"` (per TECH-388 Decision Log, not `invalid_input`); typed throw `{code: "db_unconfigured", hint?, details?}` preserves code + optional fields; `meta` passthrough from handler return. Covers TECH-388 `envelope.ts` surface. Stage 2.1 T2.1.3 of `ia/projects/mcp-lifecycle-tools-opus-4-7-audit-master-plan.md`.
acceptance: |
  5 cases covered (happy path, envelope passthrough, bare Error → internal_error, typed throw preserves code, meta passthrough); `npm run test:ia` + `npm run validate:all` green.
depends_on:
  - TECH-388
depends_on_raw: TECH-388 (envelope.ts surface)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-390** — **MCP** — envelope wrapTool unit tests (Stage 2.1 T2.1.3)
    - Type: mcp / tests
    - Files: `tools/mcp-ia-server/tests/envelope.test.ts`
    - Notes: happy path; envelope passthrough; bare `Error` → `internal_error`; typed throw preserves code (`db_unconfigured`) + optional `hint`/`details`; `meta` passthrough.
    - Acceptance: 5 cases covered; `test:ia` + `validate:all` green.
    - Depends on: TECH-388
    - Related: TECH-389, TECH-391
parent_plan: ia/projects/mcp-lifecycle-tools-opus-4-7-audit-master-plan.md
task_key: T2.1.3
