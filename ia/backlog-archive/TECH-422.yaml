id: TECH-422
type: tech / save schema
title: "Save-schema v3→v4 bump + `BudgetAllocationData` + `StateServiceZoneData` (Stage 1.3 Phase 3)"
priority: medium
status: done
section: Medium Priority
files:
  - Assets/Scripts/Managers/GameManagers/GameSaveManager.cs
notes: |
  Bump `GameSaveData.CurrentSchemaVersion` 3 → 4. Add `[Serializable] public class BudgetAllocationData { public float[] envelopePct; public int globalMonthlyCap; public int[] currentMonthRemaining; public static BudgetAllocationData Default(int cap); }`. Add `[Serializable] public class StateServiceZoneData { public int cellX, cellY; public int subTypeId; public int densityTier; }`. Add `public BudgetAllocationData budgetAllocation` + `public List<StateServiceZoneData> stateServiceZones` fields on `GameSaveData`. `Default(cap)` seeds `envelopePct = new float[7]{0.1428f, ...}` normalized to sum 1.0, `currentMonthRemaining[i] = cap * envelopePct[i]`. No migration branch yet — TECH-423.
acceptance: |
  New types compile; `CurrentSchemaVersion = 4`; new fields serializable. `unity:compile-check` green. Migration + round-trip validated by downstream tasks.
depends_on:
  - TECH-420
depends_on_raw: "**TECH-420** (Phase 2 `TryDraw` + `MonthlyReset` — consumers of `currentMonthRemaining`)"
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-422** — Save-schema v3→v4 bump + `BudgetAllocationData` + `StateServiceZoneData` (Stage 1.3 Phase 3) (2026-04-18)
    - Type: tech / save schema
    - Files: `Assets/Scripts/Managers/GameManagers/BudgetAllocationData.cs`, `Assets/Scripts/Managers/GameManagers/StateServiceZoneData.cs`, `Assets/Scripts/Managers/GameManagers/GameSaveManager.cs`
    - Spec: (removed after closure)
    - Notes: Bumped `GameSaveData.CurrentSchemaVersion` 3 → 4. New POCOs `BudgetAllocationData` (envelope pct + cap + remaining + `Default(cap)` factory — normalizes `float[7] = 1f/7f` to sum 1.0, seeds `currentMonthRemaining[i] = (int)(cap * envelopePct[i])`) and `StateServiceZoneData` (cellX/cellY/subTypeId/densityTier) under `namespace Territory.Economy`, split to sibling files (matches `BudgetAllocationService.cs` / `IBudgetAllocator.cs` layout — invariant #6 carve-out). `GameSaveData` carries `public BudgetAllocationData budgetAllocation` (nullable — TECH-423 seeds via `Default(cap)` in migration branch) + `public List<StateServiceZoneData> stateServiceZones = new List<StateServiceZoneData>();`. No migration branch (TECH-423) + no round-trip wiring (TECH-424) + no tests (TECH-425) this task.
    - Acceptance: types compile; `CurrentSchemaVersion = 4`; fields serializable; `unity:compile-check` green.
    - Depends on: **TECH-420** (archived)
parent_plan: ia/projects/zone-s-economy-master-plan.md
task_key: T1.3.5
