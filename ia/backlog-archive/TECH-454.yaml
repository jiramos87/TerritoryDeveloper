id: TECH-454
type: tech (IA infrastructure / lifecycle tooling)
title: Fold parent_phase frontmatter from open project specs (Stage 4 T4.1)
priority: high
status: closed
section: IA evolution lane
spec: ia/projects/TECH-454.md
files:
  - ia/projects/
  - ia/state/lifecycle-refactor-migration.json
notes: |
  For each open `ia/projects/{ISSUE_ID}.md` (read list from migration JSON `M3.specs` array): remove `parent_phase:` frontmatter line; ensure `parent_stage:` set to old `(parent_step, parent_stage)` concatenated as `"{step}.{stage}"`. Leave all body content (§Implementation, §Verification, §Audit etc.) untouched. Update migration JSON `M3.specs` per-file to `done` immediately after each edit (crash safety per M3 resumability contract). Read migration JSON first; never iterate `ia/projects/` directly.
acceptance: |
  All open `ia/projects/{ISSUE_ID}.md` files lack `parent_phase:` line; `parent_stage:` present with correct `"{step}.{stage}"` value; body content byte-identical outside frontmatter block; migration JSON `M3.specs` per-file entries all flipped to `done`; `npm run validate:frontmatter` passes on modified files.
depends_on: []
depends_on_raw: Stage 3 Final (TECH-453 closed; master-plan migration done; snapshot + migration JSON ground truth).
related:
  - TECH-443
  - TECH-453
  - TECH-455
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T4.1
raw_markdown: |
  - [ ] **TECH-454** — Fold `parent_phase` frontmatter from open project specs (Stage 4 T4.1)
    - Type: tech (IA infrastructure / lifecycle tooling)
    - Priority: high
    - Files: `ia/projects/`, `ia/state/lifecycle-refactor-migration.json`
    - Spec: [ia/projects/TECH-454.md](ia/projects/TECH-454.md)
    - Parent plan: [lifecycle-refactor-master-plan.md](ia/projects/lifecycle-refactor-master-plan.md) (T4.1)
    - Notes: For each open `ia/projects/{ISSUE_ID}.md` (read list from migration JSON `M3.specs` array): remove `parent_phase:` line; ensure `parent_stage:` set to `"{step}.{stage}"`. Leave body content untouched. Flip per-file `done` immediately after each edit (crash-safety).
    - Acceptance: No `parent_phase:` line in any open spec; `parent_stage:` correct; `M3.specs` per-file all `done`; `validate:frontmatter` passes.
    - Depends on: none (Stage 3 Final; TECH-453 closed)
