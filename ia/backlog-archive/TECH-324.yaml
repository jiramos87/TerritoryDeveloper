id: TECH-324
type: infrastructure / MCP tooling
title: Implement `backlog_record_validate` MCP tool (Stage 1.2 Phase 1)
priority: closed
status: closed
section: Completed (moved from BACKLOG.md, 2026-04-17)
spec: (removed after closure)
files:
  - tools/mcp-ia-server/src/tools/backlog-record-validate.ts
  - tools/mcp-ia-server/src/index.ts
notes: |
  Shipped `backlog_record_validate` MCP tool (IP5). Handler `tools/mcp-ia-server/src/tools/backlog-record-validate.ts` delegates to `validateBacklogRecord` shared core (TECH-323). Empty `yaml_body` short-circuits to `invalid_input` payload (no throw). Wrapped in `runWithToolTiming` for dashboard parity. Registered alphabetically near `registerBacklogIssue` in `tools/mcp-ia-server/src/index.ts`. Unblocks TECH-325 fixture tests.
acceptance: |
  Tool registered + boots clean; good record returns `ok: true`; bad records return shared-core rule ids (`missing_required_field`, `bad_id_format`, `bad_status`, `empty_depends_on_raw`); empty body → `invalid_input`; `npm run validate:all` green.
depends_on: []
depends_on_raw: none
related:
  - TECH-323
  - TECH-325
created: 2026-04-17
raw_markdown: |
  - [x] **TECH-324** — Implement `backlog_record_validate` MCP tool (Stage 1.2 Phase 1)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/src/tools/backlog-record-validate.ts`, `tools/mcp-ia-server/src/index.ts`
    - Notes: Handler delegates to `validateBacklogRecord` shared core (TECH-323); empty `yaml_body` → `invalid_input`; wrapped in `runWithToolTiming`; registered in MCP index. Unblocks TECH-325 fixture tests.
    - Acceptance: tool registered + boots clean; good/bad/empty paths return expected shapes; `npm run validate:all` green.
    - Related: TECH-323, TECH-325
