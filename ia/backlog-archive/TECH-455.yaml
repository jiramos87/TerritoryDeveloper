id: TECH-455
type: tech (IA infrastructure / lifecycle tooling)
title: Validate dead-spec + spec frontmatter post-fold (Stage 4 T4.2)
priority: high
status: closed
section: IA evolution lane
spec: ia/projects/TECH-455.md
files:
  - ia/projects/
  - tools/scripts/validate-dead-project-specs.ts
  - tools/scripts/validate-frontmatter.ts
notes: |
  Run `npm run validate:dead-project-specs`; triage any orphan specs flagged (spec file with no matching yaml entry in `ia/backlog/`). Run `npm run validate:frontmatter` on all modified spec files from TECH-454. Confirm no `parent_phase` field remains in any open spec (grep pass). Report exit codes + any orphans to user before moving on.
acceptance: |
  `validate:dead-project-specs` exits 0; `validate:frontmatter` exits 0 on all modified specs; `grep -r "parent_phase" ia/projects/` returns zero hits; any orphan specs either deleted or owning yaml authored (decision logged in spec Decision Log).
depends_on:
  - TECH-454
depends_on_raw: TECH-454 (parent_phase must be removed before validation).
related:
  - TECH-454
  - TECH-456
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T4.2
raw_markdown: |
  - [ ] **TECH-455** — Validate dead-spec + spec frontmatter post-fold (Stage 4 T4.2)
    - Type: tech (IA infrastructure / lifecycle tooling)
    - Priority: high
    - Files: `ia/projects/`, `tools/scripts/validate-dead-project-specs.ts`, `tools/scripts/validate-frontmatter.ts`
    - Spec: [ia/projects/TECH-455.md](ia/projects/TECH-455.md)
    - Parent plan: [lifecycle-refactor-master-plan.md](ia/projects/lifecycle-refactor-master-plan.md) (T4.2)
    - Notes: Run `npm run validate:dead-project-specs` + `npm run validate:frontmatter` post-TECH-454. Triage orphans; grep `parent_phase` → zero hits.
    - Acceptance: Both validators exit 0; grep gate clean; orphan decisions logged in Decision Log if any.
    - Depends on: **TECH-454**
