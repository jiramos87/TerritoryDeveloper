id: TECH-123
type: infrastructure / tooling scaffold
title: "`tools/sprite-gen/` folder scaffold + `requirements.txt` + README stub (2026-04-14)"
priority: closed
status: closed
section: Completed (moved from BACKLOG.md, 2026-04-13)
spec: (removed after closure)
files: 
  - tools/sprite-gen/
  - src/__init__.py
  - src/primitives/__init__.py
  - tests/fixtures/
  - out/
  - requirements.txt
  - README.md
  - .gitignore
notes: |
  Stage 1.1 Phase 1 opener. Layout per exploration §9: `src/`, `src/primitives/`, `tests/`, `tests/fixtures/`, `specs/`, `palettes/`, `out/` (gitignored). `requirements.txt` pins pillow + numpy + scipy + pyyaml. README stub points at master plan + exploration doc. Python / Unity-isolated — no runtime **C#** touched.
acceptance: |
  folder layout matches §9; `out/` gitignored; `requirements.txt` lists 4 deps; `npm run validate:all` green
depends_on: []
depends_on_raw: none
related: []
created: 2026-04-17
raw_markdown: |
  - [x] **TECH-123** — `tools/sprite-gen/` folder scaffold + `requirements.txt` + README stub (2026-04-14)
    - Type: infrastructure / tooling scaffold
    - Files: `tools/sprite-gen/` (new: `src/__init__.py`, `src/primitives/__init__.py`, `tests/fixtures/`, `out/`, `requirements.txt`, `README.md`), `.gitignore`
    - Spec: (removed after closure)
    - Notes: Stage 1.1 Phase 1 opener. Layout per exploration §9: `src/`, `src/primitives/`, `tests/`, `tests/fixtures/`, `specs/`, `palettes/`, `out/` (gitignored). `requirements.txt` pins pillow + numpy + scipy + pyyaml. README stub points at master plan + exploration doc. Python / Unity-isolated — no runtime **C#** touched.
    - Acceptance: folder layout matches §9; `out/` gitignored; `requirements.txt` lists 4 deps; `npm run validate:all` green
    - Depends on: none
