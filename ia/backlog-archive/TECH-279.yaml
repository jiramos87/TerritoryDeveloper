id: TECH-279
type: tech (scaffolding / field)
title: Add `Zone.subTypeId` sidecar field (Stage 1.1 Phase 1)
priority: high
status: closed
section: High Priority
spec: ia/projects/TECH-279.md
files: 
  - Assets/Scripts/Managers/UnitManagers/Zone.cs
notes: |
  `[SerializeField] private int subTypeId = -1;` + public getter/setter on `Zone`. Default `-1` = "RCI, no sub-type". Sidecar int vs enum expansion — keeps `ZoneType` lean (7 sub-types × 3 tiers would bloat enum to 21 entries). No save plumbing yet — v3→v4 bump lands Stage 1.3.
acceptance: |
  field + getter/setter land; default `-1` on all RCI zones; `unity:compile-check` green
depends_on: []
depends_on_raw: none
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-279** — Add `Zone.subTypeId` sidecar field (Stage 1.1 Phase 1)
    - Type: tech (scaffolding / field)
    - Files: `Assets/Scripts/Managers/UnitManagers/Zone.cs`
    - Spec: `ia/projects/TECH-279.md`
    - Notes: `[SerializeField] private int subTypeId = -1;` + public getter/setter on `Zone`. Default `-1` = "RCI, no sub-type". Sidecar int vs enum expansion — keeps `ZoneType` lean (7 sub-types × 3 tiers would bloat enum to 21 entries). No save plumbing yet — v3→v4 bump lands Stage 1.3.
    - Acceptance: field + getter/setter land; default `-1` on all RCI zones; `unity:compile-check` green
    - Depends on: none
