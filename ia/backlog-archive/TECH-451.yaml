id: TECH-451
type: tech (IA infrastructure / lifecycle tooling)
title: Canary run migrate-master-plans.ts on blip-master-plan + parser edge-case fix (Stage 2.1 Phase 1)
priority: high
status: open
section: IA evolution lane
spec: ia/projects/TECH-451.md
files:
  - tools/scripts/migrate-master-plans.ts
  - ia/projects/blip-master-plan.md
  - ia/state/lifecycle-refactor-migration.json
notes: |
  Run `npx tsx tools/scripts/migrate-master-plans.ts --only blip-master-plan.md` (fully closed plan — safe canary). Diff output against snapshot: verify task rows verbatim (Issue ids unchanged), exit criteria bullets merged into parent Stage Exit, Phase column absent from task-table header, Step+Stage pair collapsed to single Stage heading. Run `npm run validate:all`. Fix any parser edge cases surfaced by canary (nested code blocks inside task intents, missing Phase sections, inline Phase bullets that are not task rows, task-id format variants). Commit fix to migration script. Mark canary entry `done` in migration JSON.
acceptance: |
  Canary migration emits clean Stage/Task output on `blip-master-plan.md`; `npm run validate:all` green; any parser fix committed back into `migrate-master-plans.ts` before batch run; migration JSON M2 entry for `blip-master-plan.md` flipped to `done`.
depends_on:
  - TECH-450
depends_on_raw: TECH-450 (transform script must exist before canary).
related:
  - TECH-452
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T2.1.2
