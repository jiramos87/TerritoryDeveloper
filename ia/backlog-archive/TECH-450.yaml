id: TECH-450
type: tech (IA infrastructure / lifecycle tooling)
title: Author migrate-master-plans.ts transform script (Stage 2.1 Phase 1)
priority: high
status: closed
section: IA evolution lane
spec: ia/projects/TECH-450.md
files:
  - tools/scripts/migrate-master-plans.ts
  - ia/state/lifecycle-refactor-migration.json
  - ia/state/pre-refactor-snapshot/
notes: |
  Write `tools/scripts/migrate-master-plans.ts` (TypeScript, run via `npx tsx`). Reads `ia/state/lifecycle-refactor-migration.json` M2.per-file; skips files with status `done`. For each pending file, reads from `ia/state/pre-refactor-snapshot/{relative-path}` (never current). Parses markdown AST: detects Step→Stage→Phase→Task structure; maps old Step+Stage pair → new Stage (name = `"{Step name} / {Stage name}"`); merges Phase exit bullets into parent Stage Exit section; strips Phase heading rows from Phases section; drops `Phase` column from task-table headers; renumbers task ids `T{N}.{M}.{k}` → `T{stage_seq}.{k}` preserving Issue + Status verbatim. Emits to current file path (not snapshot). Flips per-file status to `done` in migration JSON immediately after emit.
acceptance: |
  Script compiles + runs under `npx tsx`; idempotent (reading snapshot + skipping done files); preserves task `Issue` + `Status` columns verbatim; flips per-file entry immediately after successful emit; does not touch files with status `done`; writes only to current path, never to snapshot.
depends_on: []
depends_on_raw: None (Stage 1.2 Final; foundational templates + rules ready).
related:
  - TECH-443
  - TECH-444
  - TECH-446
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T2.1.1
