id: TECH-686
type: tech
title: "Update callers"
priority: medium
status: closed
section: "Grid asset visual registry — Stage 2.3 (Zone S consumer migration)"
spec: 'closed: project spec removed on Stage 2.3 closeout'
files: []
notes: |
  `UIManager` / `SubTypePickerModal` / `BudgetAllocationService` / `ZoneSService` switch to registry+asset_id path for display names, cent costs, icon/prefab resolution via catalog-backed data; preserve Zone S money envelope invariants; no new singletons.
acceptance: |
  - [ ] `SubTypePickerModal` shows names/costs consistent with catalog rows.
  - [ ] `BudgetAllocationService` + `ZoneSService` use same cent values as `GridAssetCatalog` economy fields.
  - [ ] Compile clean: no dead references to old JSON-only cost path unless `#if` rollback define explicitly documented.
depends_on:
  - TECH-672
depends_on_raw: "TECH-672 — boot snapshot loader prerequisite"
related:
  - TECH-684
  - TECH-685
  - TECH-687
created: 2026-04-22
completed: "2026-04-22"
raw_markdown: |
  Update callers — UI + services use catalog-backed Zone S data
parent_plan: ia/projects/grid-asset-visual-registry-master-plan.md
task_key: T2.3.3
