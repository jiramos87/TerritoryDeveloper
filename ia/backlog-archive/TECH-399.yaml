id: TECH-399
type: mcp / infrastructure
title: "**MCP** — wrap rule + router tools + add rule_section (Stage 2.2 T2.2.2)"
priority: medium
status: closed
section: IA infrastructure
spec: ""
files:
  - tools/mcp-ia-server/src/tools/list-rules.ts
  - tools/mcp-ia-server/src/tools/rule-content.ts
  - tools/mcp-ia-server/src/tools/router-for-task.ts
notes: |
  Wrap `list-rules.ts`, `rule-content.ts`, `router-for-task.ts` in `wrapTool`. Add new `rule_section` tool (symmetric to `spec_section`, canonical params `{ rule, section, max_chars }`) alongside existing `rule_content` in `rule-content.ts`; register in MCP server index. Stage 2.2 Phase 1 T2.2.2 of `ia/projects/mcp-lifecycle-tools-opus-4-7-audit-master-plan.md`.
acceptance: |
  3 tools return via `wrapTool`; new `rule_section` tool registered + callable w/ `{ rule, section, max_chars }`; typecheck green; snapshot tests updated; `validate:all` green.
depends_on:
  - TECH-388
  - TECH-398
depends_on_raw: "TECH-388 (envelope — hard dep); TECH-398 (spec-section structured response is the template rule_section mirrors — soft)"
related:
  - TECH-398
  - TECH-400
  - TECH-401
  - TECH-402
  - TECH-403
  - TECH-404
  - TECH-405
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-399** — **MCP** — wrap rule + router tools + add rule_section (Stage 2.2 T2.2.2) (2026-04-18)
    - Type: mcp / infrastructure
    - Files: `tools/mcp-ia-server/src/tools/list-rules.ts`, `rule-content.ts`, `router-for-task.ts`
    - Spec: (removed after closure)
    - Notes: Wrap 3 tools in `wrapTool`; add `rule_section` tool (canonical `{ rule, section, max_chars }`) in `rule-content.ts`; register in MCP server index. Decision Log persisted to `ia_project_spec_journal`.
    - Acceptance: envelope wrap; `rule_section` registered + callable; typecheck + snapshot + `validate:all` green.
parent_plan: ia/projects/mcp-lifecycle-tools-opus-4-7-audit-master-plan.md
task_key: T2.2.2
