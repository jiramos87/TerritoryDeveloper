id: TECH-669
type: tech
title: "DTOs + parser"
priority: medium
status: closed
section: "Grid asset visual registry — Stage 2.2 (GridAssetCatalog runtime loader)"
spec: ia/projects/TECH-669.md
files: []
notes: |
  C# DTOs match TECH-663 snapshot top-level + row shapes. Parse JSON text w/ `JsonUtility`. No `Newtonsoft` here; split extra types into sibling partial class files if one file is unwieldy. Places `GridAssetCatalog` DTOs under `Assets/Scripts/...` w/ XML summaries on public fields used by the loader. Aligns w/ grid-asset master plan Step 2 Stage 2.2 + exploration §8 snapshot envelope.
acceptance: |
  - [ ] DTO public fields line up 1:1 w/ `schemaVersion` + ordered arrays the export emits.
  - [ ] `JsonUtility` parse passes on a fixture string copied from `catalog:export` output (document fixture path in §Findings).
  - [ ] No Newtonsoft dependency; no `FindObjectOfType` in parse path.
depends_on:
  - TECH-663
depends_on_raw: "TECH-663"
related:
  - TECH-670
  - TECH-671
  - TECH-672
  - TECH-673
created: 2026-04-22
completed: \"2026-04-22\"
raw_markdown: |
  DTOs + parser — GridAssetCatalog JsonUtility DTOs + parse helper.
parent_plan: ia/projects/grid-asset-visual-registry-master-plan.md
task_key: T2.2.1
