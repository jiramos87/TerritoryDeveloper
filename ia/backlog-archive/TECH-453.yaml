id: TECH-453
type: tech (IA infrastructure / lifecycle tooling)
title: Post-batch validate:all + frontmatter sweep + flip migration JSON M2 done (Stage 2.1 Phase 2)
priority: high
status: closed
closed: 2026-04-19
section: IA evolution lane
spec: ia/projects/TECH-453.md
files:
  - ia/state/lifecycle-refactor-migration.json
  - ia/projects/
notes: |
  Run `npm run validate:all` on full repo after batch migration completes. Fix any validation failures — most likely: task-id format checks in `tools/mcp-ia-server/` validators (`T{stage}.{k}` vs old `T{step}.{stage}.{k}`), stale parser expectations in `web/lib/plan-parser.ts` (deferred to Stage 3.2 but may fire on validate:web), dead-project-specs complaints on any spec whose parent plan anchor shifted. Run `npm run validate:frontmatter` on all migrated master plans. Flip migration JSON M2 phase status to `done`.
acceptance: |
  `npm run validate:all` exits 0 on full repo post-batch; `npm run validate:frontmatter` green on all migrated plans; migration JSON `phases.M2.status` = `done`; all M2 per-file entries = `done`.
depends_on:
  - TECH-452
depends_on_raw: TECH-452 (batch migration must complete before validate sweep).
related: []
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T2.1.4
