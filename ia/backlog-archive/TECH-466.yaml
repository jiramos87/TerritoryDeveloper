id: TECH-466
type: chore / config
title: "Flip `DEBUG_MCP_COMPUTE=1` on by default in `.mcp.json` env"
priority: low
status: closed
completed: 2026-04-19
section: Low Priority
spec: ia/projects/TECH-466.md
files:
  - .mcp.json
notes: |
  Add `DEBUG_MCP_COMPUTE: "1"` under `mcpServers.territory-ia.env` in `.mcp.json` — preserve existing `REPO_ROOT` entry, do NOT overwrite env block. Stderr path is stdio-safe (MCP spec: stdout = protocol, stderr = free). Per-tool cold-start timing surfaces in transport logs — pays for itself on first-of-day sessions. Zero runtime C# impact; no invariants touched. Source: `docs/session-token-latency-audit-exploration.md` §B7 + Theme 0 ship order (B7 first). NB-2: if output voluminous, gate on secondary flag later — audit recommends unconditional, trust audit. FREEZE-safe (no master-plan decomposition required — standalone `/project-new`).
acceptance: |
  `.mcp.json` `mcpServers.territory-ia.env` carries `DEBUG_MCP_COMPUTE: "1"` alongside `REPO_ROOT`; JSON still valid; Claude Code restart surfaces per-tool compute timing on stderr; `npm run validate:all` green.
related: []
created: 2026-04-19
raw_markdown: |
  - [x] **TECH-466** — Flip `DEBUG_MCP_COMPUTE=1` on by default in `.mcp.json` env
    - Type: chore / config
    - Files: `.mcp.json`
    - Spec: [ia/projects/TECH-466.md](ia/projects/TECH-466.md)
    - Notes: Add `DEBUG_MCP_COMPUTE: "1"` under `mcpServers.territory-ia.env`; preserve `REPO_ROOT`. Stderr stdio-safe. Source: `docs/session-token-latency-audit-exploration.md` §B7 (Theme 0 ship-order #1). FREEZE-safe standalone.
    - Acceptance: env block carries flag alongside `REPO_ROOT`; JSON valid; `validate:all` green.
