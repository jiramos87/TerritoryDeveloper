id: TECH-343
type: IA / skill docs
title: Wire MCP tools into `stage-file` + `project-new` skills (Stage 1.3 Phase 1)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ""
files: 
  - ia/skills/stage-file/SKILL.md
  - ia/skills/project-new/SKILL.md
notes: |
  Edited `ia/skills/stage-file/SKILL.md` + `ia/skills/project-new/SKILL.md` — reserve-id step names `reserve_backlog_ids` MCP tool first (`count: N` batch in `stage-file`, `count: 1` in `project-new`); `backlog_record_validate` step inserted after yaml body authoring, before disk write; bash fallback (`tools/scripts/reserve-id.sh`) kept as "if MCP unavailable" alternative. No behavior change beyond tool-preference. Stage 1.3 Phase 1 of `backlog-yaml-mcp-alignment-master-plan.md`.
acceptance: |
  Both skill bodies name `reserve_backlog_ids` first + `backlog_record_validate` before disk write; bash fallback preserved; caveman prose; `npm run validate:all` green.
depends_on: 
  - TECH-324
  - TECH-326
depends_on_raw: TECH-324, TECH-326 (MCP tools must exist before skills can cite them — hard gate; both archived)
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-343** — Wire MCP tools into `stage-file` + `project-new` skills (Stage 1.3 Phase 1) (2026-04-18)
    - Type: IA / skill docs
    - Files: `ia/skills/stage-file/SKILL.md`, `ia/skills/project-new/SKILL.md`
    - Spec: (removed after closure)
    - Notes: Shipped MCP-first reserve-id + pre-write yaml lint wording in both skills — `reserve_backlog_ids` named first (batch in `stage-file`, single in `project-new`); `backlog_record_validate` inserted between yaml authoring and disk write; bash fallback preserved. Caveman prose. Closes Stage 1.3 Phase 1.
    - Acceptance: both skills cite MCP tools first; fallback preserved; `validate:all` green.
    - Depends on: TECH-324, TECH-326 (MCP tools — archived)
    - Related: TECH-344, TECH-345, TECH-346
