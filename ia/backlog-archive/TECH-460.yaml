id: TECH-460
type: tech (IA infrastructure / lifecycle tooling / MCP)
title: "Author plan_apply_validate MCP tool (Stage 5 T5.3)"
priority: high
status: closed
completed: 2026-04-19
section: IA evolution lane
spec: ia/projects/TECH-460.md
files:
  - tools/mcp-ia-server/src/tools/plan-apply-validate.ts
  - tools/mcp-ia-server/src/index.ts
notes: |
  Add `plan_apply_validate` tool to MCP server. Handler signature `(section_header: string, target_path: string) → {ok: boolean, found: boolean, tuple_count: number, error?: string}`; reads `target_path`; searches for `## {section_header}` heading; counts `{operation, target_path, target_anchor, payload}` tuple lines below it; returns `found: false` when heading absent. Registers tool alongside `plan-apply-pair-contract.md` reference. Appliers call this gate before running to assert §Plan anchor presence + non-empty tuple list.
acceptance: |
  `plan_apply_validate` handler present at `tools/mcp-ia-server/src/tools/plan-apply-validate.ts`; returns correct shape on valid path + known §Plan section; returns `found: false` on missing section; returns `tuple_count: 0` on empty section; unit tests cover all three branches; `validate:all` green.
depends_on:
  - TECH-448
depends_on_raw: "TECH-448 (plan-apply-pair-contract.md authored — tuple shape reference)."
related:
  - TECH-458
  - TECH-461
created: 2026-04-19
parent_plan: ia/projects/lifecycle-refactor-master-plan.md
task_key: T5.3
raw_markdown: |
  - [x] **TECH-460** — Author `plan_apply_validate` MCP tool (Stage 5 T5.3)
    - Type: tech (IA infrastructure / lifecycle tooling / MCP)
    - Priority: high
    - Files: `tools/mcp-ia-server/src/tools/plan-apply-validate.ts`, `tools/mcp-ia-server/src/index.ts`
    - Spec: [ia/projects/TECH-460.md](ia/projects/TECH-460.md)
    - Parent plan: [lifecycle-refactor-master-plan.md](ia/projects/lifecycle-refactor-master-plan.md) (T5.3)
    - Notes: Add `plan_apply_validate(section_header, target_path) → {ok, found, tuple_count, error?}` handler. Reads target, counts §Plan tuple lines, returns validation result.
    - Acceptance: Handler present; correct shape; three-branch unit tests green.
    - Depends on: **TECH-448**
