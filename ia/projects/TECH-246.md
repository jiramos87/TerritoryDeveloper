---
purpose: "TECH-246 — Glossary Blip bootstrap row update for visible-UI path + SfxMutedKey."
audience: both
loaded_by: ondemand
slices_via: none
---
# TECH-246 — Glossary Blip bootstrap row update for visible-UI path + SfxMutedKey

> **Issue:** [TECH-246](../../BACKLOG.md)
> **Status:** Draft
> **Created:** 2026-04-16
> **Last updated:** 2026-04-16

## 1. Summary

Update `ia/specs/glossary.md` **Blip bootstrap** row (line ~208) — append to definition: boot-time `SfxMutedKey` restore semantics + visible-volume-UI path via `BlipVolumeController`. Spec reference column stays `ia/specs/audio-blip.md §5.1`, `§5.2` — no change (bootstrap lifecycle sections unchanged). Satisfies Stage 4.2 Exit bullet "**Blip bootstrap** notes visible-volume-UI path alongside headless PlayerPrefs binding" + "`ia/specs/glossary.md` **Blip bootstrap** row updated with `SfxMutedKey` boot-time restore + `BlipVolumeController` visible-UI path".

## 2. Goals and Non-Goals

### 2.1 Goals

1. **Blip bootstrap** row definition extended — boot-time `SfxMutedKey` read + clamp + visible-UI path `BlipVolumeController`.
2. No change to spec-ref column (still points `ia/specs/audio-blip.md §5.1`, `§5.2`).
3. No change to Index row (Audio list at line 32 already contains "Blip bootstrap" — term name unchanged).
4. `npm run validate:all` green — dead-spec-refs + frontmatter + IA indexes all clean.

### 2.2 Non-Goals

1. Add new glossary rows — `SfxMutedKey` + `BlipVolumeController` are implementation details, not canonical terms (spec-winning rule: if a term is only in code not in spec prose, no glossary row needed).
2. Edit `ia/specs/audio-blip.md §5.1` / §5.2 prose — settings-UI lifecycle doc lives in `blip-master-plan.md` Step 4; audio-blip.md covers runtime pipeline only.
3. Authoritative-spec update — `audio-blip.md §5.1` component map does not list `BlipVolumeController` (Step 4 surface, not Step 1–3 runtime).

## 3. User / Developer Stories

| # | Role | Story | Acceptance criteria |
|---|------|-------|---------------------|
| 1 | Developer | As contributor searching glossary for "Blip bootstrap", I see that Options UI primes from same `PlayerPrefs` keys | Definition mentions `BlipVolumeController` + `SfxMutedKey` |
| 2 | Agent | As spec-kickoff agent resolving "how does mute persist?", glossary row answers in-line without hitting impl code | Boot-time `SfxMutedKey` clamp described in definition |

## 4. Current State

### 4.1 Domain behavior

Current `ia/specs/glossary.md` line 208 **Blip bootstrap** definition covers headless `PlayerPrefs` volume bind + scene-load suppression. Does not mention visible-UI path (landed Stage 4.1) or boot-time mute restore (landing TECH-245 this stage).

### 4.2 Systems map

- `ia/specs/glossary.md` line 208 — single row edit.
- Index row line 32 — unchanged (term "Blip bootstrap" already listed).
- `ia/specs/audio-blip.md` — no edit.
- `tools/mcp-ia-server/data/glossary-*.json` — regenerated by `npm run generate:ia-indexes` (part of `validate:all`).

## 5. Proposed Design

### 5.2 Architecture / implementation

**Current row (line 208 verbatim):**

```md
| **Blip bootstrap** | Persistent GameObject at `MainMenu.unity` root; `DontDestroyOnLoad` on `Awake`. Hosts Catalog / Player / MixerRouter / Cooldown child slots. Scene-load suppression: `BlipEngine.Play` returns early until `BlipCatalog.Awake` sets ready flag (lands Step 2 / Stage 1.2). Prevents boot-race clicks during `MainMenu → Game.unity` transition. | `ia/specs/audio-blip.md §5.1`, `§5.2` |
```

**Proposed row (spec-ref unchanged):**

```md
| **Blip bootstrap** | Persistent GameObject at `MainMenu.unity` root; `DontDestroyOnLoad` on `Awake`. Hosts Catalog / Player / MixerRouter / Cooldown child slots. Scene-load suppression: `BlipEngine.Play` returns early until `BlipCatalog.Awake` sets ready flag (lands Step 2 / Stage 1.2). Prevents boot-race clicks during `MainMenu → Game.unity` transition. Boot-time: also reads `SfxMutedKey` (`PlayerPrefs.GetInt`) and clamps dB to −80 if muted, ahead of mixer apply. Visible-volume-UI path: `BlipVolumeController` (mounted on `OptionsPanel`) primes slider/toggle from `PlayerPrefs` on `OnEnable` and writes back on change. | `ia/specs/audio-blip.md §5.1`, `§5.2` |
```

**Caveman prose applies** (agent-output-caveman §authoring surfaces — glossary row definitions are reference-spec body).

## 7. Implementation Plan

### Phase 1 — Glossary edit + validate

- [ ] Open `ia/specs/glossary.md`.
- [ ] Replace line 208 **Blip bootstrap** row per §5.2 proposed prose.
- [ ] Run `npm run validate:all` — green (dead-spec-refs + frontmatter + fixtures + IA indexes).

## 7b. Test Contracts

| Acceptance / goal | Check type | Command or artifact | Notes |
|---|---|---|---|
| Row updated | Manual diff | `git diff ia/specs/glossary.md` | Single row change at line 208 |
| IA indexes regen | Node | `npm run validate:all` | Chains `generate:ia-indexes --check` |
| Full chain green | Node | `npm run validate:all` | repo root |

## 8. Acceptance Criteria

- [ ] **Blip bootstrap** row definition contains "Boot-time: also reads `SfxMutedKey`" + "Visible-volume-UI path: `BlipVolumeController`".
- [ ] Spec-ref column unchanged (`ia/specs/audio-blip.md §5.1`, `§5.2`).
- [ ] Index row line 32 unchanged.
- [ ] `npm run validate:all` exit 0.

## 10. Cross-refs

- Parent: [`blip-master-plan.md`](blip-master-plan.md) Step 4 Stage 4.2 Phase 2.
- Depends on: TECH-243 + TECH-244 (both archived) + [`TECH-245.md`](TECH-245.md) (behaviors glossary row describes).
- Closes Step 4 Stage 4.2 (last task).

## Open Questions

None — doc-only; Stage 4.2 Exit bullet verbatim.
