id: TECH-424
type: tech / save wiring
title: "Save/load round-trip wiring for `BudgetAllocationService` (Stage 1.3 Phase 3)"
priority: medium
status: closed
section: Medium Priority
closed: 2026-04-18
spec: (removed after closure)
files:
  - Assets/Scripts/Managers/GameManagers/BudgetAllocationService.cs
  - Assets/Scripts/Managers/GameManagers/GameSaveManager.cs
notes: |
  Add `BudgetAllocationData CaptureSaveData()` + `void RestoreFromSaveData(BudgetAllocationData)` methods on `BudgetAllocationService`. `GameSaveManager.SaveGame` calls capture → writes to `GameSaveData.budgetAllocation`. `GameSaveManager.LoadGame` calls restore post-migration. Verify envelope pct + globalMonthlyCap + currentMonthRemaining survive save → load → identity. No S zones restored yet (placement lands in Step 2 — `stateServiceZones` list stays empty).
acceptance: |
  Post save/load, service state (pct + cap + remaining) byte-identical. Migration from v3 save seeds defaults + restore round-trip is no-op. `unity:compile-check` green. Round-trip regression test lands in TECH-425.
depends_on:
  - TECH-418
  - TECH-422
  - TECH-423
depends_on_raw: "**TECH-418** (service skeleton), **TECH-422** (schema fields), **TECH-423** (migration branch)"
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-424** — Save/load round-trip wiring for `BudgetAllocationService` (Stage 1.3 Phase 3) (2026-04-18)
    - Type: tech / save wiring
    - Files: `Assets/Scripts/Managers/GameManagers/BudgetAllocationService.cs`, `Assets/Scripts/Managers/GameManagers/GameSaveManager.cs`
    - Spec: (removed after closure)
    - Notes: `Capture` / `Restore` methods on service; `SaveGame` / `LoadGame` call them post-migration. S zones list stays empty (Step 2 territory).
    - Acceptance: state round-trips byte-identical; `unity:compile-check` green.
    - Depends on: **TECH-418**, **TECH-422**, **TECH-423**
parent_plan: ia/projects/zone-s-economy-master-plan.md
task_key: T1.3.7
