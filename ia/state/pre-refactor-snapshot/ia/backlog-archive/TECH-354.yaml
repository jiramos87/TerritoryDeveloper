id: TECH-354
type: tech (web RSC page)
title: Author `/dashboard/releases/[releaseId]/progress` RSC progress tree page (Stage 7.2 T7.2.4)
priority: medium
status: closed
section: Web platform lane
spec: (removed at closeout — journal persisted Decision Log)
files: 
  - web/app/dashboard/releases/[releaseId]/progress/page.tsx
notes: |
  RSC. `resolveRelease(params.releaseId)` → `notFound()` on null. `loadAllPlans()` + `getReleasePlans` + per-plan `computePlanMetrics` + `buildPlanTree` + `deriveDefaultExpandedStepId`. Renders `Breadcrumb` (Dashboard › Releases › {release.label} › Progress) + `<PlanTree>` per plan. Reserved `/rollout` comment (B1 guard — URL 404s by default, no filesystem stub). Full-English headings (caveman exception). Stage 7.2 Phase 2 of `web-platform-master-plan.md`.
acceptance: |
  `web/app/dashboard/releases/[releaseId]/progress/page.tsx` RSC resolves release or 404s; renders `Breadcrumb` + `<PlanTree>` per plan; default-expanded id from metrics; reserved `/rollout` comment present; `npm run validate:web` green.
depends_on: 
  - TECH-340
  - TECH-342
  - TECH-352
depends_on_raw: TECH-340..TECH-342 (archived — Stage 7.1 shapers), TECH-352 (archived — PlanTree Client island)
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-354** — Author `/dashboard/releases/[releaseId]/progress` RSC progress tree page (Stage 7.2 T7.2.4) (2026-04-18)
    - Type: tech (web RSC page)
    - Files: `web/app/dashboard/releases/[releaseId]/progress/page.tsx`
    - Spec: (removed at closeout — journal persisted Decision Log)
    - Notes: RSC. `resolveRelease` → `notFound()`. `loadAllPlans` + `getReleasePlans` + `computePlanMetrics` + `buildPlanTree` + `deriveDefaultExpandedStepId`. `Breadcrumb` + `<PlanTree>` per plan. Reserved `/rollout` comment (B1). Full-English headings.
    - Acceptance: resolves or 404s; `<PlanTree>` per plan; default-expand via metrics; reserved comment; `validate:web` green.
    - Depends on: **TECH-340**..**TECH-342** (archived), **TECH-352** (archived — PlanTree)
    - Related: **TECH-351**, **TECH-352**, **TECH-353**
