id: TECH-328
type: infrastructure / MCP tooling
title: Implement `backlog_list` MCP tool (Stage 1.2 Phase 3)
priority: high
status: closed
completed: 2026-04-17
section: Backlog YAML ↔ MCP alignment program
spec: (removed after closure)
files:
  - tools/mcp-ia-server/src/tools/backlog-list.ts
  - tools/mcp-ia-server/src/index.ts
notes: |
  MCP tool `backlog_list` — input `{ section?, priority?, type?, status?, scope? (default "open") }`, load via `parseAllBacklogIssues`, apply filters in-memory, return `{ issues, total }` ordered by id desc. Replaces ad-hoc Grep patterns agents use today. Stage-shared context: closes Phase 3 of Stage 1.2 alongside TECH-329 tests. Consumes `priority` field added to `ParsedBacklogIssue` in Stage 1.1 (TECH-295).
acceptance: |
  Tool registered; each filter dim works standalone + combined; scope switch (open / archive / all) returns correct sets; empty-result case returns `{ issues: [], total: 0 }`; id-desc ordering stable.
depends_on: []
depends_on_raw: none
related:
  - TECH-329
created: 2026-04-17
raw_markdown: |
  - [x] **TECH-328** — Implement `backlog_list` MCP tool (Stage 1.2 Phase 3) (2026-04-17)
    - Type: infrastructure / MCP tooling
    - Files: `tools/mcp-ia-server/src/tools/backlog-list.ts`, `tools/mcp-ia-server/src/index.ts`
    - Spec: (removed after closure)
    - Notes: Shipped `backlog_list` MCP tool (IP4). Handler loads records via `parseAllBacklogIssuesWithMeta(repoRoot, scope)`; applies AND filters (section substring + priority/type/status exact case-insensitive); sorts prefix-alphabetic then numeric id desc; returns `{ scope, total_searched, result_count, issues, parseErrorCount? }`. Issue row mirrors `backlog_search` shape (id, title, type, status, section, priority, related, created, notes-200). Wrapped in `runWithToolTiming`. Registered via `registerBacklogList` in `index.ts`. Replaces ad-hoc Grep enumeration. Unblocks TECH-329 fixture tests.
    - Acceptance: tool registered; filters standalone + combined; scope switch works; empty-result case; id-desc ordering stable; `validate:all` green.
    - Related: TECH-329
