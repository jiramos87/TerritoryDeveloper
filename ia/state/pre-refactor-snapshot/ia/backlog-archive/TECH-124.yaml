id: TECH-124
type: infrastructure / DSP (geometry)
title: "`canvas.py` canvas sizing + Unity pivot math (2026-04-14)"
priority: closed
status: closed
section: Completed (moved from BACKLOG.md, 2026-04-13)
spec: (removed after closure)
files: 
  - tools/sprite-gen/src/canvas.py
notes: |
  Stage 1.1 Phase 1 second task. `canvas_size(fx, fy, extra_h=0) → (w, h)` per exploration §4 baseline `(fx+fy)*32`; `pivot_uv(canvas_h) → (0.5, 16/canvas_h)`. Pure functions, docstring cites §4. Must match **Tile dimensions** (tileWidth=1, tileHeight=0.5) so emitted PNGs align w/ Unity isometric diamond at PPU=64.
acceptance: |
  both functions match §4 examples; docstrings cite source; `npm run validate:all` green
depends_on: 
  - TECH-123
depends_on_raw: "**TECH-123**"
related: []
created: 2026-04-17
raw_markdown: |
  - [x] **TECH-124** — `canvas.py` canvas sizing + Unity pivot math (2026-04-14)
    - Type: infrastructure / DSP (geometry)
    - Files: `tools/sprite-gen/src/canvas.py`
    - Spec: (removed after closure)
    - Notes: Stage 1.1 Phase 1 second task. `canvas_size(fx, fy, extra_h=0) → (w, h)` per exploration §4 baseline `(fx+fy)*32`; `pivot_uv(canvas_h) → (0.5, 16/canvas_h)`. Pure functions, docstring cites §4. Must match **Tile dimensions** (tileWidth=1, tileHeight=0.5) so emitted PNGs align w/ Unity isometric diamond at PPU=64.
    - Acceptance: both functions match §4 examples; docstrings cite source; `npm run validate:all` green
    - Depends on: **TECH-123**
