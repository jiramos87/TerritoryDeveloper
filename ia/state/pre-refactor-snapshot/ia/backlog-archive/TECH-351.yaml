id: TECH-351
type: tech (web client component)
title: Author `web/components/TreeNode.tsx` — recursive render primitive (Stage 7.2 T7.2.1)
priority: medium
status: closed
section: Web platform lane
spec: ia/projects/TECH-351.md
files: 
  - web/components/TreeNode.tsx
notes: |
  Recursive render of `TreeNodeData`. Status-colored glyph — chevron for branches, `●` for task leaves. Label + `{done}/{total}` count summary. `<button aria-expanded={isExpanded} aria-controls={childListId}>` for non-leaf toggles (a11y). Leaf tasks show Issue id when present (not `_pending_`). Consumes existing `BadgeChip` status token CSS classes. Props: `node: TreeNodeData, expanded: Set<string>, onToggle: (id: string) => void`. Stage 7.2 Phase 1 of `web-platform-master-plan.md` — Step 7 release-scoped progress view.
acceptance: |
  `web/components/TreeNode.tsx` renders recursively; glyph + label + count per node; `aria-expanded` + `aria-controls` on branch toggles; leaf issue ids render when not `_pending_`; `npm run validate:web` green.
depends_on: 
  - TECH-339
  - TECH-342
depends_on_raw: TECH-339..TECH-342 (Stage 7.1 shapers — all archived; TreeNodeData type + metrics required)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-351** — Author `web/components/TreeNode.tsx` — recursive render primitive (Stage 7.2 T7.2.1)
    - Type: tech (web client component)
    - Files: `web/components/TreeNode.tsx`
    - Spec: `ia/projects/TECH-351.md`
    - Notes: Recursive `TreeNodeData` render. Status glyph (chevron/`●`) + label + `{done}/{total}`. `<button aria-expanded aria-controls>` branch toggles. Leaf issue id when not `_pending_`. Consumes `BadgeChip` token classes. Props: `node, expanded: Set<string>, onToggle`.
    - Acceptance: recursive render; glyph + count per node; a11y attrs on branches; `validate:web` green.
    - Depends on: TECH-339..TECH-342 (Stage 7.1 shapers — all archived)
    - Related: TECH-352, TECH-353, TECH-354
