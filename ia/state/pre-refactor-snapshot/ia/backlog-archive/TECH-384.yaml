id: TECH-384
type: infrastructure / template
title: Add 2-field mirror to spec template (Stage 3.2 Phase 1)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-384.md
files:
  - ia/templates/project-spec-template.md
notes: |
  Edit `ia/templates/project-spec-template.md` frontmatter block — add `parent_plan: {{PARENT_PLAN_PATH}}` + `task_key: {{T_KEY}}` rows. Block comment above names the 2-field mirror rule + that step/stage/phase derive from `task_key` parser (no 5-field frontmatter). Lazy — populated on next `/kickoff`, never retroactive. Source: Step 3 Stage 3.2 Phase 1.
acceptance: |
  Template carries `parent_plan` + `task_key` placeholder rows + block-comment rule; `validate:all` green; existing specs unaffected (lazy / non-retroactive).
depends_on:
  - TECH-365
depends_on_raw: TECH-365 (writer path — hard gate)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-384** — Add 2-field mirror to spec template (Stage 3.2 Phase 1)
    - Type: infrastructure / template
    - Files: `ia/templates/project-spec-template.md`
    - Spec: `ia/projects/TECH-384.md`
    - Notes: add `parent_plan` + `task_key` placeholder rows + block-comment naming the 2-field mirror rule; step/stage/phase derived from `task_key`; lazy — not retroactive.
    - Acceptance: template carries both rows + rule comment; `validate:all` green.
    - Depends on: TECH-365 (writer path — hard gate)
    - Related: TECH-385, TECH-386, TECH-387
