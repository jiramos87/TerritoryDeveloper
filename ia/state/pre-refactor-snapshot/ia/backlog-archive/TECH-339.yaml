id: TECH-339
type: tech (web data layer)
title: Author `web/lib/releases.ts` — Release registry + resolver (Stage 7.1 T7.1.1)
priority: closed
status: closed
section: Completed (moved from BACKLOG.md, 2026-04-18)
spec: (removed after closure)
files: 
  - web/lib/releases.ts
notes: |
  Hand-maintained release registry. `Release` interface (`id`, `label`, `umbrellaMasterPlan`, `children: string[]`) + `resolveRelease(id) → Release | null` lookup + seeded `releases` const w/ single `full-game-mvp` row (9 children verbatim from `docs/web-platform-post-mvp-extensions.md` §1 Examples; utilities/landmarks/distribution absent per tracker rows 8/9/11). Header cites `ia/projects/full-game-mvp-rollout-tracker.md` as source of truth + drift warning. Pure data layer — no routes, no UI, no auth. Decision Log persisted to `ia_project_spec_journal`.
acceptance: |
  `web/lib/releases.ts` exports `Release` interface + `resolveRelease` + seeded `releases` array; header cites rollout tracker doc; `npm run validate:web` green; `npm run validate:all` green.
depends_on: []
depends_on_raw: none
related: []
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-339** — Author `web/lib/releases.ts` — Release registry + resolver (Stage 7.1 T7.1.1) (2026-04-18)
    - Type: tech (web data layer)
    - Files: `web/lib/releases.ts`
    - Spec: (removed after closure)
    - Notes: Hand-maintained release registry. `Release` interface (`id`, `label`, `umbrellaMasterPlan`, `children: string[]`) + `resolveRelease(id) → Release | null` + seeded `full-game-mvp` row (9 children verbatim from `docs/web-platform-post-mvp-extensions.md` §1 Examples; utilities/landmarks/distribution absent per tracker rows 8/9/11). Header cites `ia/projects/full-game-mvp-rollout-tracker.md` + drift warning. Pure data layer. Decision Log persisted to `ia_project_spec_journal`.
    - Acceptance: registry compiles; `resolveRelease` returns seeded row + null on miss; `validate:web` green; `validate:all` green.
    - Related: TECH-340, TECH-341, TECH-342
