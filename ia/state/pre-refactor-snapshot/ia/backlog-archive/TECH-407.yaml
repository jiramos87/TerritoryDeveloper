id: TECH-407
type: infrastructure / scripts
title: CLI wrapper + dual-mode flag (Stage 3.3 Phase 1)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ""
files:
  - tools/validate-parent-plan-locator.mjs
  - package.json
  - tools/mcp-ia-server/src/parser/parent-plan-validator.ts
notes: |
  Closed. CLI wrapper `tools/validate-parent-plan-locator.mjs` shipped — loads TECH-406 core via compiled dist (`tools/mcp-ia-server/dist/parser/parent-plan-validator.js`); `--strict` / `--advisory` flags (mutually exclusive; default advisory). Advisory exit 0 always (drift count printed only on drift); strict exit 1 on error + full `ERROR:` / `WARN:` lines. `validate:parent-plan-locator` script registered in root `package.json`, chained into `validate:all` AFTER `compute-lib:build` (dist-import order) as advisory. Decision log lived in ia/projects/TECH-407.md (deleted on closeout); history in git.
acceptance: |
  CLI clean advisory (exit 0); `--strict` exits 1 on injected drift; script registered; `validate:all` green with advisory chain wire-in.
depends_on:
  - TECH-406
depends_on_raw: TECH-406 (shared validator core — hard gate)
related:
  - TECH-406
  - TECH-408
  - TECH-410
created: 2026-04-18
closed: 2026-04-18
raw_markdown: |
  - [x] **TECH-407** — CLI wrapper + dual-mode flag (Stage 3.3 Phase 1)
    - Type: infrastructure / scripts
    - Files: `tools/validate-parent-plan-locator.mjs`, `package.json`, `tools/mcp-ia-server/src/parser/parent-plan-validator.ts`
    - Notes: Wraps TECH-406 core via compiled dist; `--strict` / `--advisory` flags (default advisory); `validate:parent-plan-locator` script chained into `validate:all` advisory-only (strict flip deferred to Step 6).
    - Acceptance: CLI clean advisory; `--strict` exits 1 on drift; script registered; `validate:all` green.
    - Depends on: **TECH-406** (archived — shared validator core)
    - Related: TECH-408, TECH-410
parent_plan: ia/projects/backlog-yaml-mcp-alignment-master-plan.md
task_key: T3.3.2
