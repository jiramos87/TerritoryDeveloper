id: TECH-386
type: infrastructure / scripts
title: Implement backfill driver (Stage 3.2 Phase 2)
priority: medium
status: closed
section: Backlog YAML ↔ MCP alignment program
spec: ia/projects/TECH-386.md
files:
  - tools/scripts/backfill-parent-plan-locator.mjs
  - ia/backlog/
  - ia/projects/
notes: |
  `tools/scripts/backfill-parent-plan-locator.mjs` (new) — loads all `ia/backlog/*.yaml`; for each, parses `title` suffix `(Stage X.Y Phase Z)`; walks `ia/projects/*master-plan*.md` task tables via regex `\| T[\d.]+ \| .* \| \*\*{id}\*\*` for forward `parent_plan` resolution; on resolve, writes v2 fields via schema-v2 writer (T3.1.3 / TECH-365). Supports `--dry-run` + `--skip-unresolvable`. Idempotent (second run = zero writes). Open-yaml only this stage; archive deferred to Step 6. Source: Step 3 Stage 3.2 Phase 2.
acceptance: |
  Driver runs clean on current `ia/backlog/*.yaml`; `--dry-run` previews w/o writes; `--skip-unresolvable` logs skip reasons; idempotent (2nd run = zero writes); `validate:all` green post-backfill.
depends_on:
  - TECH-365
  - TECH-384
depends_on_raw: TECH-365 (schema-v2 writer — hard gate), TECH-384 (template mirror — soft)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-386** — Implement backfill driver (Stage 3.2 Phase 2)
    - Type: infrastructure / scripts
    - Files: `tools/scripts/backfill-parent-plan-locator.mjs`, `ia/backlog/`, `ia/projects/`
    - Spec: `ia/projects/TECH-386.md`
    - Notes: idempotent one-shot pass over open yaml; parses title suffix + walks plan task tables for forward `parent_plan` + `task_key` resolution; `--dry-run` + `--skip-unresolvable`.
    - Acceptance: clean run on current open yaml; idempotent; dry-run previews; skip logs counts; `validate:all` green.
    - Depends on: TECH-365 (schema-v2 writer — hard), TECH-384 (template mirror — soft)
    - Related: TECH-384, TECH-385, TECH-387
parent_plan: ia/projects/backlog-yaml-mcp-alignment-master-plan.md
task_key: T3.2.3
