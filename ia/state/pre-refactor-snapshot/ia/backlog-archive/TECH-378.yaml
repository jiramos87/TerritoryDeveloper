id: TECH-378
type: web / code
title: "**Web** — `globals.css` `@theme` `--ds-*` extension + reduced-motion media (Stage 8.1 T8.1.4)"
priority: medium
status: closed
section: Web platform
spec: ""
files:
  - web/app/globals.css
notes: |
  Extend `web/app/globals.css` `@theme` block — append `--ds-*` CSS custom properties: `--ds-font-size-display` … `--ds-font-size-mono-meta` (type scale), `--ds-spacing-2xs` … `--ds-spacing-layout` (spacing), `--ds-duration-instant` … `--ds-duration-deliberate` + `--ds-duration-reduced-motion: 0ms` (motion), `--ds-text-*` / `--ds-surface-*` / `--ds-accent-*` semantic aliases. All prefixed `ds-*` (B1 guard — no collision with existing `--color-*` / `--spacing-*` / `--text-*`). Add `@media (prefers-reduced-motion: reduce)` rule setting all `--ds-duration-*` to `0ms`. Stage 8.1 T8.1.4 of `ia/projects/web-platform-master-plan.md`.
acceptance: |
  `globals.css` `@theme` block has all `--ds-*` properties (font-size + spacing + duration + text + surface + accent); existing `--color-*` / `--spacing-*` / `--text-*` untouched; `prefers-reduced-motion` media collapses all `--ds-duration-*` to `0ms`; `npm run validate:web` + `npm run validate:all` green.
depends_on:
  - TECH-377
depends_on_raw: "TECH-377 (token TS source-of-truth — hard; CSS values mirror TS const)"
related:
  - TECH-375
  - TECH-376
  - TECH-377
created: 2026-04-18
raw_markdown: |
  - [x] **TECH-378** — **Web** — `globals.css` `@theme` `--ds-*` extension + reduced-motion media (Stage 8.1 T8.1.4) (2026-04-18)
    - Type: web / code
    - Files: `web/app/globals.css`
    - Spec: (removed — unfiled via TECH-411 Phase 1 reset)
    - Notes: Append `--ds-font-size-*` + `--ds-spacing-*` + `--ds-duration-*` + `--ds-text/surface/accent-*` to `@theme`; B1 guard (no collision w/ existing `--color-*` / `--spacing-*` / `--text-*`); `@media (prefers-reduced-motion: reduce)` collapses durations to `0ms`.
    - Acceptance: all `--ds-*` present; existing untouched; reduced-motion collapse active; `validate:web` + `validate:all` green.
    - Depends on: TECH-377 (TS const mirror — hard)
    - Related: TECH-375, TECH-376, TECH-377
