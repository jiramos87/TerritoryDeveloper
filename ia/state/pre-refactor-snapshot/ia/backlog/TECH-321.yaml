id: TECH-321
type: audio / scene
title: "**Music** — MainMenu scene placement + compile verify"
priority: medium
status: open
section: Stage 1.1 — Mixer extension + persistent bootstrap
spec: ia/projects/TECH-321.md
files:
  - Assets/Scenes/MainMenu.unity
notes: |
  Place `MusicBootstrap.prefab` instance at `MainMenu.unity` root (sibling to existing `BlipBootstrap` prefab instance). Commit `MainMenu.unity` scene diff. Run `npm run unity:compile-check` — verify green. Manual smoke: launch MainMenu → `MusicBootstrap.Instance != null` post-Awake → warn log absent (mixer ref wired). Invariant #4 satisfied at stage close. Stage 1.1 Phase 3 T1.1.6 of `ia/projects/music-player-master-plan.md`.
acceptance: |
  `MainMenu.unity` scene hosts `MusicBootstrap` prefab instance at root (sibling to `BlipBootstrap`). `npm run unity:compile-check` green. Manual smoke: `MusicBootstrap.Instance != null` after Awake; no null-mixer warn in log. Invariant #4 satisfied.
depends_on:
  - TECH-320
depends_on_raw: TECH-320 (prefab must exist before scene placement)
related: []
created: 2026-04-18
raw_markdown: |
  - [ ] **TECH-321** — **Music** — MainMenu scene placement + compile verify (Stage 1.1 Phase 3)
    - Type: audio / scene
    - Files: `Assets/Scenes/MainMenu.unity`
    - Spec: `ia/projects/TECH-321.md`
    - Notes: Stage 1.1 Phase 3 T1.1.6 of music-player orchestrator. Place `MusicBootstrap.prefab` at `MainMenu.unity` root (sibling to `BlipBootstrap`). Compile verify + manual smoke — `Instance != null`, no null-mixer warn.
    - Acceptance: Scene diff committed; `unity:compile-check` green; smoke passes; invariant #4 satisfied.
    - Depends on: TECH-320 (prefab must exist before scene placement)
parent_plan: ia/projects/music-player-master-plan.md
task_key: T1.1.6
